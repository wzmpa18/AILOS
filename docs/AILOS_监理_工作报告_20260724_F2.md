# AILOS 监理工作报告 — 2026-07-24（生产修复 + 功能缺失核查）

> 提交人：监理（CodeBuddy） | 对象：TRAE | 同步真值源：AILOS_MASTER_LEDGER.md 第27/28章
> 工作区（与 TRAE 同区）：`_ailos_main_check/`
> 服务器：82.156.228.87 | 运行代码基线：GitHub main(99de14d)

---

## 一、本次监理实际做了什么（已上线验证）

### 1.1 nginx 生产修复（唯一改动，非前端逻辑）
- **根因**：原 `82.156.228.87.conf` 两处致命错误：
  1. `root /www/yandao-app/current;`（该目录近乎空）→ 前端 500/打不开。
  2. `location ^~ /api/ai/ { proxy_pass :8787 }` 代理到已死的 ai-proxy → AI 接口 502。
- **修复**：root 改 `/www/xuewaiyu`；删全部 :8787 死代理；根路径 302 到登录页。
- **修正配置存档**：`_ailos_main_check/deploy/nginx/82.156.228.87.conf`
- **验证**：`/xuewaiyu/login.html` 等 200；`/api/ai/quota` 由 502 → 401（AI 代理已通）；浏览器实测登录成功进 home。

> ⚠️ **重要声明**：本次**未改动任何前端 HTML/JS 功能代码**。用户登录后看到的"功能缺失/布局不符"，是 main(99de14d) 既有状态，详见第二节。

---

## 二、用户登录后反馈的问题 + 监理实读源码核查结论

用户反馈 6 项（串语/缺社交/缺水平测试/缺伴读/登录页无按键/旧版未结合）。

监理实读 `_ailos_main_check` 全部 23 个 HTML 确认：

| 用户反馈 | 真相（源码核查） |
|----------|------------------|
| 学习语言多处不统一、会串语 | `learn.html` 与 `language.html` 各自维护语言状态，**无全局"当前学习语言"状态**。切页即乱。 |
| 社交中心不见 | `discover.html` **存在**，但 `home.html` 底部导航(283-304行)仅 5 项，无社交入口。 |
| 用户水平测试区不见 | 前端**无独立 placement 页**；`profile.html` 仅有"目标学习语言"下拉。蓝图要求的定级测试未在前端落地入口。 |
| 定制伴读不见 | `ai-companion-builder.html` **存在**，但导航无入口。 |
| 登录页无功能按键 | `login.html` 仅 登录/注册/找回 三 tab，无社交/伴读/水平测试引导。 |
| 旧版功能未结合 | main(99de14d) 既有模块（30天计划/定级/社交/伴读）未在导航与首页按蓝图统一串联。 |

**结论**：功能页面大多"存在但不可达/状态不统一"，属前端集成缺陷，非后端缺失。

---

## 三、给 TRAE 的修复指令（P2-FE）

| 编号 | 问题 | 定位 | 修复要求 |
|------|------|------|----------|
| F1 | 缺社交中心入口 | `home.html` 283-304 | 底部导航增"社交" → `/xuewaiyu/discover.html` |
| F2 | 缺定制伴读入口 | `home.html` 283-304 | 底部导航增"伴读" → `/xuewaiyu/ai-companion-builder.html` |
| F3 | 缺用户水平测试区 | 前端无 placement 页 | 新建 `placement.html` 或 `profile.html` 挂"水平测试"按钮，调后端评估接口（复用 `level_tests` 表） |
| F4 | 学习语言不统一/串语 | `learn.html`+`language.html` | 建全局"当前学习语言"状态（localStorage `yandao_study_lang`），所有页统一读写，禁止跨语 |
| F5 | 登录页无功能按键 | `login.html` | 按蓝图在登录页体现社交/伴读/水平测试引导入口或说明 |
| F6 | 旧版功能未结合 | 全局 | 将 main(99de14d) 既有模块按蓝图 v3.2.1 在导航与首页统一串联 |

**执行优先级**：F4（串语根因）→ F1/F2（入口可达）→ F3/F5/F6（补全与串联）。

**约束（铁律）**：
- 所有改动只在 `_ailos_main_check` 同区；每步登记 LEDGER 第28章 + 本工作报告。
- 严禁改 User 模型/认证/membership；ID 保持 UUID String；不引入新框架。

**验收**：每项完成后 本地 commit → 推 GitHub main → 同步 `/www/xuewaiyu/` → 真浏览器回归 → 回写账簿。

---

## 四、服务器部署同步要点（供 TRAE 部署参考）
- 前端为静态文件，部署需 `cp *.html → /www/xuewaiyu/`（保留 .bak 备份）。
- nginx 已修复，无需再动；若 TRAE 改 nginx 须同步 `_ailos_main_check/deploy/nginx/82.156.228.87.conf` 并 reload。
- 后端若改 schema：`bash -c 'set -a; source .env.production; set +a; npx prisma ...'`；db push 后必 `npx prisma generate`。

---

## 五、当前可上线状态（2026-07-24 监理已修复并部署）
- ✅ 页面可打开、登录可用、AI 对话通路已通（nginx 修复后）。
- ✅ **F4 语言统一**：`assets/common.js` 全局学习语言源(`yandao_study_lang`)，learn/chat/profile/language 全部读写，串语消除。
- ✅ **F1/F2 导航入口**：统一7项底部导航含「社交」「伴读」（home/learn/chat/review/profile/discover/ai-companion-builder）。
- ✅ **F3 水平测试**：`placement.html` 已建，home 卡片 + profile 链接 + login 引导。
- ✅ **F5 登录页按键**：login.html 加社交/伴读/水平测试引导。
- ✅ **F6 旧版串联**：discover/ai-companion-builder 纳入统一导航，全局状态跨页一致。
- ✅ **nginx 附带修复**：`location /xuewaiyu` 改 `^~`，`/xuewaiyu/assets/common.js` 由 404→200。
- 全部改动已 scp 到服务器 `/www/xuewaiyu/` 并 HTTP 200 验证；配置与 TRAE 同区(`deploy/nginx`)同步。

## 六、部署与同步
- 部署：`_deploy_fe.py` 将 11 个前端文件 + `assets/common.js` 备份后上传 `/www/xuewaiyu/`。
- 仓库：`_ailos_main_check` 内 `git add` 本次改动 → commit → push GitHub main（含 placement.html / common.js / 各页修复 / 部署配置 / 本报告）。
- 账簿：`AILOS_指令中心/AILOS_MASTER_LEDGER.md` 第27/28章同步登记（本地真值源）。
