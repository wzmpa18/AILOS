# 增补 1：AILOS 系统双版本拓扑图（现状版 vs 目标治理版）

> 文档性质：P0 强制增补项，现状版如实暴露所有违规旁路，目标版严格对齐 v3.0 宪法五层内核
> 关联宪法：CoreOS 第 0 编四大铁律、第 6 章 Brain 唯一出口

---

## 1.1 现状版拓扑图（如实暴露所有违规旁路）

```mermaid
graph TB
    subgraph FRONTEND["前端 27 HTML"]
        P["profile/learn/vocabulary/sentences<br/>placement/chat/translate/companion"]
    end

    subgraph FACADE["Controller 层"]
        C["33条路由 /api/*"]
    end

    subgraph SERVICES["Service 层"]
        VOCAB["vocabularyService"]
        GRAMMAR["grammarService"]
        READING["readingService"]
        SPEAKING["speakingService"]
        SOCIAL["socialService"]
        BILLING["billingService"]
    end

    subgraph GATEWAY["AI 网关层"]
        AG["aiGateway ✅"]
        AIS["aiService ⚠️含直连路径"]
    end

    subgraph BYPASS["🔴 违规旁路（一级违宪）"]
        B1["aiService._callDirect()<br/>直连混元 API<br/>⚠️ 绕过 LanguageGuard/QuotaManager/AuditLogger"]
        B2["hunyuanVisionAdapter<br/>直连混元 OCR<br/>⚠️ 独立 API Key，无额度/审计"]
        B3["voiceService<br/>直连腾讯云 TTS/ASR<br/>⚠️ 无语言校验/无配额扣减"]
        B4["deviceRiskService<br/>直写 Redis<br/>⚠️ 绕过 SSOT 唯一真值源"]
        B5["aiQuotaService<br/>直写 Redis<br/>⚠️ 绕过 SSOT 唯一真值源"]
    end

    subgraph MODEL["外部模型"]
        HY["混元 API<br/>tokenhub.tencentmaas.com"]
        TX["腾讯云 TTS/ASR<br/>tts.tencentcloudapi.com"]
    end

    subgraph DATA["数据层"]
        DB["PostgreSQL 85表"]
        REDIS["Redis"]
    end

    P --> C
    C --> SERVICES
    C --> AG
    AG --> AIS
    AIS -->|"✅ 合规路径"| MODEL
    AIS -->|"🔴 一级违宪"| HY
    B2 --> HY
    B3 --> TX
    B4 --> REDIS
    B5 --> REDIS
    SERVICES --> DB

    style B1 fill:#ff4444,color:#fff
    style B2 fill:#ff4444,color:#fff
    style B3 fill:#ff4444,color:#fff
    style B4 fill:#ffaa00,color:#000
    style B5 fill:#ffaa00,color:#000
```

### 现状版违规旁路清单

| # | 违规路径 | 违规类型 | 严重等级 | 文件位置 | 整改阶段 |
|---|---------|---------|---------|---------|---------|
| 1 | `aiService._callDirect()` → 混元 API | 绕过 Brain | 🔴 一级违宪 | `src/services/aiService.js:96-125` | P2 内核落地 |
| 2 | `hunyuanVisionAdapter` → 混元 OCR | 绕过 Brain | 🔴 一级违宪 | `src/services/ocr/hunyuanVisionAdapter.js:28` | P2 内核落地 |
| 3 | `voiceService` → 腾讯云 TTS | 绕过 Brain | 🔴 一级违宪 | `src/services/voiceService.js:120` | P2 内核落地 |
| 4 | `voiceService` → 腾讯云 ASR | 绕过 Brain | 🔴 一级违宪 | `src/services/voiceService.js:170` | P2 内核落地 |
| 5 | `deviceRiskService` → Redis.setex | 违反 SSOT | 🟡 二级故障 | `src/services/deviceRiskService.js:203` | P2 内核落地 |
| 6 | `aiQuotaService` → Redis.set | 违反 SSOT | 🟡 二级故障 | `src/services/aiQuotaService.js` | P2 内核落地 |
| 7 | `authService` → Redis.setex | 违反 SSOT | 🟡 二级故障 | `src/services/authService.js` | P2 内核落地 |

---

## 1.2 目标治理版拓扑图（严格对齐 v3.0 宪法五层内核）

```mermaid
graph TB
    subgraph L5["五层：应用接入层"]
        PAGES["27 HTML 页面<br/>仅调用 CoreOS Facade"]
    end

    subgraph L4["四层：Core OS 调度内核（治理心脏）"]
        FACADE["CoreOS Facade 统一门面<br/>（唯一前端入口）"]
        EVENT["EventBus 事件总线<br/>（唯一跨模块通道）"]
        SM["GlobalStateMachine<br/>（用户/学习状态管控）"]
        DG["DependencyGuard<br/>（运行时依赖拦截）"]
    end

    subgraph L3["三层：AILOS Brain AI 内核（唯一 AI 出口）"]
        PC["PromptCenter"]
        MC["MemoryCenter"]
        CB["ContextBuilder"]
        LG["LanguageGuard ⚠️ 串语拦截"]
        MR["ModelRouter 分级调度"]
        OV["OutputValidator"]
        QM["QuotaManager"]
        CM["CostManager"]
        RE["RetryEngine"]
        AL["AuditLogger"]
        VS["VoiceSubsystem<br/>（TTS/ASR 统一管控）"]
        OCR["OCRSubsystem<br/>（视觉识别统一管控）"]
    end

    subgraph L2["二层：六大领域（DDD 完全隔离）"]
        VOCAB["词汇领域"]
        GRAMMAR["语法领域"]
        READING["阅读领域"]
        SPEAKING["口语领域"]
        SOCIAL["社交领域"]
        BILLING["付费领域"]
    end

    subgraph L1["一层：数据持久内核（SSOT 唯一真值源）"]
        DB[("PostgreSQL 85表<br/>全部 SSOT 标记")]
        REDIS[("Redis 只读缓存<br/>仅 CoreOS 可写")]
    end

    PAGES -->|"唯一调用路径"| FACADE
    FACADE --> L4
    L4 -->|"唯一 AI 出口"| L3
    L3 --> L2
    L2 --> L1
    L4 -.->|"缓存失效（唯一写入口）"| REDIS
    L1 -.->|"只读副本"| REDIS

    %% 标注：不存在任何旁路
    L5 -.-x|"🔒 禁止直连"| L3
    L5 -.-x|"🔒 禁止直连"| L1
    L2 -.-x|"🔒 禁止直连"| L3
```

---

## 1.3 整改路线图（现状→目标）

```mermaid
flowchart LR
    NOW["现状：6条违规旁路"] --> P2["P2：Core OS 内核落地"]
    P2 --> FIX1["删除 aiService._callDirect()"]
    P2 --> FIX2["visionAdapter 重构为 Brain OCRSubsystem"]
    P2 --> FIX3["voiceService 重构为 Brain VoiceSubsystem"]
    P2 --> FIX4["所有 Redis 写操作收归 CoreOS"]
    P2 --> TARGET["目标：0 旁路，全路径经 Brain"]
    TARGET --> SCAN["P3 全盘扫描核验"]
    SCAN --> FROZEN["账簿标记 FROZEN"]
```

---

> **增补 1 版本**: v1.0 | **归档路径**: AILOS_指令中心/系统图谱/增补1_双版本拓扑图.md
