# 监理报告：旧版(xuewaiyu)功能盘点 + TRAE M0–M3 交付验收 — 2026-07-25

> 审计人：总工程师/监理（Local）
> 依据：桌面 5 份旧版报告（工程架构/功能综合/工作日记/用户要求汇总/产品与学习体系）+ 旧版源码实读 `C:\Users\ZhuanZ\Downloads\xuewaiyu--xuewaiyu`（React 前端 74 个 jsx + 后端 42 个 js）

---

## 第一部分：TRAE「M0–M3 全部完成」验收结论 — **不可验收（代码未推送）**

| 检查项 | 结果 |
|--------|------|
| GitHub `wzmpa18/AILOS` 远端分支 | **只有 main 一个分支** |
| `origin/main` HEAD | `9306863`（监理 F1–F6 提交，7-24） |
| TRAE 声称的 13 个新文件（dailyPlanService.js / speaking.html / intentRouter.js / costCircuitBreaker.js 等） | **在 GitHub 上 0 个存在** |
| TRAE 声称的 7 个修改文件（aiGateway.js / aiController.js / schema.prisma 等） | **远端无任何新提交** |

**结论：`验收状态 = FAIL-未交付`。** TRAE 的报告写得漂亮（方向、文件命名、验证口径都符合任务书 V4.0），但**一行代码都没推到 GitHub main**。监理无法核实真伪——鉴于 TRAE 有过「虚构 V7.1」前科，在代码可审计之前，**一律不采信**。

**给 TRAE 的硬性要求**：立即 `git push origin main`（或推 feature 分支发 PR），并附 Q1–Q8 书面回执（任务书 V4.0 第一步，至今未交）。**推送前，M0–M3 视为 0% 完成。**

---

## 第二部分：旧版功能盘点（实读源码，非凭报告）

### 2.1 旧版游戏中心（用户记忆"七八个"，实际源码如下）
| 游戏 | 旧版文件 | 状态 | 迁移决定 |
|------|---------|------|---------|
| 言道地球村 | VillagePage.jsx + game/(VillageScene/MapData/QuestSystem等6文件) | 可玩(主打) | **不迁移**（用户明确排除） |
| 词汇猎手（单词↔释义快速配对） | WordMatchGame.jsx | 可玩 | 迁移 |
| 语法方块（句子填空挑战） | FillBlankGame.jsx | 可玩 | 迁移 |
| 密室逃脱（限时答题闯关） | SpeedQuizGame.jsx | 可玩 | 迁移 |
| 魔法星球 | 无实现（route:null，"敬请期待"） | 占位 | 迁移为 AI 生成玩法（见指令） |
| 游戏奖励系统 | GameRewardModal.jsx + gameRewardService.js（连胜/统计/评测时长） | 可用 | 迁移 |
| **词语接龙** | **源码中未找到**（全局搜索"接龙"0 命中） | 不存在 | 列入新增（AI 天然适合做接龙裁判） |

> 说明：旧版实际是 **4 个可玩游戏 + 1 个占位**。用户记忆中的"词语接龙"等其余游戏在源码中不存在，可能是规划未实现。处理方式：迁移现有 4 个 + 新增「词语接龙」等 AI 游戏，凑齐游戏中心 6-8 个。

### 2.2 旧版新用户引导流（关键：水平测试只做一次）
```
Splash(介绍页) → 隐私弹窗 → 伴读问候/设置 → 语言选择 → 测试引导(TestIntro)
→ 水平定级测试(LevelTest, 仅注册后一次) → 目标设定(GoalSetting)
→ 轨道选择(TrackSelect) → 计划生成(PlanGeneration) → 首页
```
- 完成后 `hasCompletedSetup=true`，以后不再进引导，直接 `/home`。
- 配套 `StudyMethodsPage`（学习方法推荐：影子跟读法/延迟跟读等，根据定级结果推荐）。
- **现版差距**：placement.html 是我临时建的硬编码 6 题，且无"只测一次+推荐学习方法"闭环。

### 2.3 旧版完整页面清单（62 页）与迁移分级
**A 级（核心学习，必迁）**：LearnPage(关卡地图)、LearningPage/EnhancedLearningPage(每关卡=10词+5听音选词+5组句+1短文阅读+5跟读)、ExamPage/ExamLearningPage(考试)、WrongBookPage(错题本)、ShadowingPage(影子跟读)、ReadingPage(阅读)、GrammarPage(语法)、SentenceBankPage(句库)、RadioPage(AI电台)、LevelTest+TestIntro+StudyMethods(定级+方法推荐)、GoalSetting+TrackSelect+PlanGeneration(目标/轨道/计划)。

**B 级（游戏与激励，必迁）**：GamesPage、WordMatch/FillBlank/SpeedQuiz 三游戏、GameReward 奖励系统、AchievementsPage(成就)、LeaderboardPage(排行榜)、ShopPage(商店)、WardrobePage(衣橱/装扮)。

**C 级（社交与伴读，必迁，蓝图挂钩）**：SocialPage(社交Tab)、MessagesPage(消息Tab)、GroupChatPage/GroupCreatePage(群聊)、AIChatPage、AIScenePage(AI场景)、RolePlayPage(角色扮演)、AICompanionSelect/CompanionSetup/CompanionGreeting/CompanionCall/LearningPartnerStage(伴读全链路)、VoiceSettings/VoiceCustom(语音)。

**D 级（增长与商业化，迁移但支付冻结）**：VIPPage/MembershipPage、ReferralPage(推荐)、CommissionPage(佣金)、DistributePage(分销)、SharePosterPage(分享海报)。

**E 级（合规，必迁）**：PrivacyPage/PrivacyModal、TermsPage、**备案号展示**。

**排除**：VillagePage + game/ 目录（地球村，用户明确不要）；AdminPage(后台另议)。

### 2.4 旧版 UI 骨架（用户本次点名要求）
- `BottomNav.jsx`：5 Tab（首页/学习/社交/消息/我的），**全局常驻**（App 容器级挂载，非每页自嵌）——现版每页自嵌 HTML 有遗漏，须改为统一组件常驻。
- 每个子页 `page-header` 带 **左上角 ← 返回键**（navigate(-1)）——现版多数页面缺失。
- Splash 介绍页：游客体验/注册/登录三入口——现版**游客体验点击无效**（BUG-019），登录按钮须最大，底部挂**备案号**。
- 注册页：现版写死 +86——须改为**国旗+国家代码完整选择器**（BUG-020）。

### 2.5 防交叉污染原则（迁移铁律）
旧版内容全部**硬编码**在 `data/languageData.js`、`learningContent.js`（仅中/英/日几套写死题）——这正是蓝图禁止的。因此迁移 = **只迁交互与玩法外壳，内容一律走现版 aiGateway/蓝本框架 AI 生成**，绝不复制旧 data 文件；语言跟随用户 GLOI 语言档案，杜绝串语。

---

## 第三部分：结论
1. TRAE M0–M3：**FAIL-未交付**，先推代码+交 Q1–Q8 回执，才重启验收。
2. 旧版功能迁移：已形成完整清单与分级（上表），配套指令见 `TRAE_任务书_V4.1_旧版功能迁移.md`。
3. 新增用户要求编号：BUG-019(游客体验失效)、BUG-020(区号只有+86)、REQ-01(底部导航常驻)、REQ-02(全局返回键)、REQ-03(介绍页三按钮层级+备案号)、REQ-04(定级只测一次+方法推荐)、REQ-05(社交按蓝图补全)。
