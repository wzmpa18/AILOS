# 工作报告：首次引导全链路（Stage 2/3/4）开发·部署·验收

- **报告日期**：2026-07-26
- **执行角色**：监理（Local，用户授权 SSH 部署）
- **对应账簿**：AILOS_MASTER_LEDGER.md 第33章
- **GitHub**：wzmpa18/AILOS，main = `7a537c1`
- **服务器**：82.156.228.87 `/www/xuewaiyu-backend`，HEAD = `7a537c1`（已对齐），pm2 online

---

## 一、任务来源与目标

用户要求按蓝图规划实现"登录/注册后的首次引导"完整链路，并对照《AILOS项目双宪法全套合规开发文档》检查完善：

> 身份选择(机构/个人，个人默认学习) → 选学习语言 → 自评级别 → 对应级别 10 题定级测试(6选择+2听力+2发音) → 评级打分推荐学习计划 → 学习目的(考级/商务/生活交流) → 用户用几句话构建 AI 伴读角色(性格/声音/口头禅) → 用各国指定/热门教材+大咖学习方法，结合用户一段话描述的重点方向，AI 打造 30 天个性化学习计划。

## 二、交付清单

### 2.1 提交记录（均已推送 GitHub main 并部署）
| 哈希 | 内容 |
|------|------|
| `41397c2` | Stage2-4 首次引导全链路 + CompanionProfile 表 + SUP-05/SUP-06 修复 + 登录注册闸门接入 onboarding |
| `7a537c1` | BUG-018 同修：register.html 存 token 兼容 tokens.accessToken |

### 2.2 后端：新增 `/api/onboarding` 全链路（均需 JWT 鉴权）
| 接口 | 功能 | 落库 |
|------|------|------|
| `GET /status` | 断点续走：各步完成态 + onboardingComplete | — |
| `POST /identity` | 身份 personal/teacher/school/enterprise | UserIdentity |
| `POST /language` | 选语言(日/英/韩/法/西/德/中)+自评级别 | UserLearningLanguage |
| `POST /placement/start` | 混元 AI 实时出 10 题(6选择+2听力audioText+2发音referenceText)，AI 失败回退内置题库 | 会话存 UserIdentity.metadata.placementSession |
| `POST /placement/submit` | **后端权威评分**：四维(词汇/语法/听力/口语)+总分+评级(N5-N1/A1-C1/TOPIK/HSK)+教材推荐 | LearningProgress + LearningAbilityModel |
| `POST /goal` | 学习目的 exam/business/daily_life/travel/interest + 目标级别 | LearningGoal |
| `POST /companion` (+GET) | 用户几句话 → AI 构建伴读：名字/性格/声音/口头禅/问候语/emoji/systemPrompt | **CompanionProfile（新表）** |
| `POST /plan/generate` | 用户重点描述 → AI 结合权威教材(新标日/GENKI/新概念/延世等)+方法论(艾宾浩斯/影子跟读/i+1/费曼) 生成 30 天计划 | DailyLearningPlan × 30 |

新文件：`src/services/onboardingService.js`、`src/server/routes/onboarding.js`。

### 2.3 前端
- **新建 `onboarding.html`**（8 步向导）：听力题浏览器 TTS 朗读、发音题 SpeechRecognition 麦克风识别（不支持可跳过不阻断）；评级结果含四维雷达；支持断点续走。
- **登录/注册闸门**：无 `yandao_onboarding_done` 且无 `yandao_level_assessment` → 跳 onboarding；老用户不受影响。

### 2.4 数据库（纯 additive）
- 新增 `CompanionProfile` 表；User 仅加反向关系。已在服务器 `db push + generate`。
- **未改** User 认证/membership/UUID。

### 2.5 顺带修复的 3 个真实 Bug
| 编号 | 问题 | 影响 | 修复 |
|------|------|------|------|
| SUP-05 | dailyPlanService 引用不存在的 `prisma.dailyPlan` | `/api/plan/*` 必 500 | 重写对齐 DailyLearningPlan，现 200 |
| SUP-06 | speechEvaluateService 引用不存在的 `prisma.speechEvaluation` | 语音评测保存必崩 | 对齐 SpeechEvaluationRecord，对外响应结构不变 |
| BUG-018同修 | register.html 只存 result.token（后端实际返回 tokens.accessToken） | 注册后 token 存不上被弹回登录 | 兼容两种字段 |

## 三、验收记录（真实服务器、真实账号，两轮）

### 3.1 首验（2026-07-25）与复检（2026-07-26）均全绿
全链路 API 实测（账号 13480010005）：
1. 登录 200（token 于 `tokens.accessToken`）
2. identity → personal ✓
3. language → ja/beginner ✓
4. placement/start → **混元 AI 实时出题成功**，10 题结构完整（6 choice + 2 listening 带 audioText + 2 speaking 带 referenceText）✓
5. placement/submit → 46 分评 **N5**，四维分数(词汇50/语法50/听力50/口语30)+推荐语+逐题明细 ✓
6. goal → daily_life，目标 N4 ✓
7. companion → AI 生成「小樱 🌸 温柔邻家姐姐，口头禅"慢慢来我陪着你"」✓
8. plan/generate → 30 天计划入库，含教材清单 ✓
9. status → `onboardingComplete: true` ✓
10. `/api/plan/today` → 200，返回 Day1 任务（SUP-05 修复生效）✓

### 3.2 基础设施复检（2026-07-26）
- 服务器 HEAD：`7a537c1`（与 GitHub main 一致，本轮已 `git reset --hard origin/main` 对齐）
- pm2：online，重启计数无增长，pm2-error.log 无新错误
- 数据库：CompanionProfile 1 行、DailyLearningPlan 30 行
- 页面（真实入口 `http://82.156.228.87/xuewaiyu/`）：home 38.5KB / login 39.3KB / register 47.6KB / onboarding 29.3KB / placement 9.9KB，**全部 200**
- `register.html` 线上 grep `tokens.accessToken` = 3 处，修复确认落地

### 3.3 澄清（避免误报）
复检中曾出现 curl 404/500，定位为**测试方法问题**：本机 curl 带 `Host: www.xuewaiyu.online` 头会命中 nginx 默认站点（真实 server_name 是 `82.156.228.87`）。用真实 IP 入口访问全部 200。**线上无故障。**

## 四、遗留事项（不阻断）
1. 发音题评分现为"完成即基础分+文本相似度"，未接 speechEvaluateService 深度评测（后续增强项）。
2. 听力题依赖浏览器 TTS，无真人音频资产。
3. 服务器→GitHub 偶发网络中断，部署可用 SFTP 直传兜底（本轮已实操验证可行）。

## 五、结论
- 用户描述的引导流程 **100% 实现并上线**，两轮真实验收全绿。
- 可签 `RC_READY_ONBOARDING`。阻断项：无。
- 铁律恪守：未改认证/membership、UUID 不变、schema 纯 additive、无新框架。
