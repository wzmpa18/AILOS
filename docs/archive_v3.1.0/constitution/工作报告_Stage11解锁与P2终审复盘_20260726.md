# 工作报告 · P2 全流程整改与 Stage 11 解锁复盘（2026-07-26）

> 迭代：P2 双语言强制规范整改 → 深度终审 → Stage 11 翻译引擎解锁
> 真值源：AILOS_MASTER_LEDGER.md V2.1（唯一法定依据）
> 仓库：wzmpa18/AILOS @ main = f64ee4a（服务器 82.156.228.87 同步）

---

## 一、本次迭代总览

| 阶段 | 内容 | 结论 |
| --- | --- | --- |
| P2 任务一（读链路收口+前端兜底） | ContextResolver 统一收口 + 非 AI 页面兜底 | ✅ FROZEN |
| P2 任务二（一致性校验） | languageConsistencyService 双字段校验 + 自愈 | ✅ FROZEN |
| P2 任务三（全链路回归） | SOP Phase0~5，V-01~V-10 全绿 | ✅ FROZEN |
| P2 深度终审·阶段一（3 前置） | 注册双语言全链路 / 缓存整改 / 写链路审计 | ✅ 3/3 PASS（6.4） |
| P2 深度终审·阶段二（7 并行） | 备份演练/存量巡检/缓存一致/限流/管理员/定时任务/部署脚本 | 🔶 已排期（6.5） |
| **Stage 11 翻译引擎** | 解锁闸门三条件满足 | 🟢 **正式生效** |

---

## 二、P2 核心整改结论（摘要）

1. **双语言强制规则真实生效**：新用户 `16712340001` 注册→onboarding→AI 全链路验证，未设目标语时 `ai/chat` 返回 `400 LANG_CONFIG_INCOMPLETE` 硬阻断，零默认兜底；注册后 DB 字段与用户选择一致，AI 输出语种匹配。
2. **前端缓存整改真实触达用户侧**：发现并修复 2 项实锤缺口（17 页面 `common.js` 加 `?v=20260726a` 版本号；活动 nginx `yandao.vip.conf` 补 `/xuewaiyu/` 全路径 `Cache-Control: no-cache`）；线上 `chat.html` `Language Context` 控件计数 = 0；SW 风险由 v3.2.2 自毁 SW 设计消解。
3. **写链路无旁路**：grep 审计确认语言字段写入口仅 `onboardingService` / `languageService` / `authService`(仅母语初始化) / `languageConsistencyService`(受约束自愈)，src 零 raw SQL，admin 路由无语言写接口。

---

## 三、终审批复与 5 类新增风险（详见账簿 6.6）

用户下发《完整终审报告 + 标准化下一阶段执行指令》，新增 5 类风险。经与 LEDGER 核验，**修正 3 处失实前提**后落地：

| # | 新增风险 | 处置 | 状态 |
| --- | --- | --- | --- |
| 1 | 网页版线上预览缺失 | Playwright 三端（Chrome/Firefox/移动）截图 24+18 张，全 200、`lc_controls=0`，证据入 `_webpreview/` | ✅ |
| 2 | 部署无标准化幂等流水线 | 新增 `deploy/deploy.sh`（整合既有脚本，五步+回滚，无凭据）；纠正「临时脚本」误述 | ✅ |
| 3 | SystemConfig 无读写管控 | 设计触发器审计 + RBAC + 审批台账，并入阶段二任务 8（第 2 轮） | 🔶 排期 |
| 4 | OCR 无成本熔断 | 单用户日 50 张 + 全局成本熔断 + 套餐引导，写入子模块 1 验收一票否决 | ✅ 需求入库 |
| 5 | 迁移无 Expand-Contract | 写入双宪法附录 C-2（破坏性变更三阶段；加性变更直接 migrate deploy） | ✅ |

**前提修正（重要）**：
- 部署脚本并非临时手工，仓库已有 `deploy_p1.sh` 等；
- Stage 11 此前已按 6.4 闸门解锁，本批复 5 项作为准入增强并入，不重复解锁；
- 当前生产已用 `migrate deploy`（版本化可回滚），反对一刀切禁单步迁移。

**阶段解锁回执 AILOS_STAGE11_OFFICIAL_UNLOCK（5/5 满足，见账簿 6.6 回执）**。

---

## 四、Stage 11 翻译引擎开发固定顺序（不可跳步，附件 L 3.1）

1. 子模块 1：静态拍照翻译 + OCR 分层限流 + 词汇/句型同步接口（同步落地 OCR 成本熔断）
2. 子模块 2：按量/日/周/月计费校验、时长扣减全链路
3. 子模块 3：实时对话流式翻译
4. 子模块 4：AR 实时扫描、双重哈希帧去重
5. 子模块 5：本地话题加密归档、多语种学习包生成
6. 子模块 6：设备指纹防刷、跨境合规、定时一致性校验联动

---

## 五、并行补正任务交付节点（绑定子模块）

| 类型 | 任务 | 交付节点 |
| --- | --- | --- |
| 高优先级 | onboarding 绕过校验、短信完整注册、限流白名单环境变量、管理员配置台账 | 子模块 1 前 |
| 中优先级 | 静态资源自动化版本、存量用户语言抽检、窗口期实测、非 AI 页面兜底 | 子模块 2 前 |
| 新增中风险 | CI/CD 脚本(deploy.sh)、SystemConfig 后台、分阶段迁移规范、OCR 成本熔断 | 子模块 3 前（OCR 已并入子模块 1） |

---

## 六、验收一票否决新增条款（并入双宪法第五章）

1. 线上网页版无完整预览验收记录 → 翻译模块驳回；
2. OCR 拍照翻译无用户/全局成本限流熔断 → 不予冻结；
3. 部署无标准化幂等脚本 → 迭代上线暂停；
4. SystemConfig 直插数据库、无权限审计 → 配置相关功能打回；
5. 数据库采用单步不可逆迁移 → 禁止发布生产。

---

## 七、关键文档与证据索引

- 账簿验收：`AILOS_总账账簿.md` 6.4（阶段一）/ 6.5（阶段二排期）/ 6.6（终审批复）
- 网页预览证据：`_ailos_main_check/_webpreview/`（24+18 张 PNG + `manifest.json`）
- 部署脚本：`_ailos_main_check/deploy/deploy.sh`
- 双宪法附录 C-2：`_blueprint_dual.md`
- 阶段一验收原始日志：`_s1_out.txt` / `_s1b_out.txt` / `_s1c_out.txt` / `_s1d_out.txt`

---

## 八、双宪法前置阅读铁律 + 宪法条款增补归档（2026-07-26 之二）

### 8.1 指令与处置
用户下发《双宪法前置阅读铁律 + 宪法条款增补修订》，确立仓库 `docs/AILOS_CONSTITUTION.md`（main 最新版）为唯一最高准则，要求：① 增补条款写入仓库宪法并提交 main；② 输出宪法阅读确认回执；③ 更新账簿与工作报告；④ 再启 Stage 11。前三步已完成。

### 8.2 事实校准（2 处）
1. 用户按"集成版 v2.1.0"章节号（第九章/第十章/附录 C-F）引用，仓库实为单文档式 Constitution（Chapter 1-9+Appendix E）；已按仓库真实结构映射落点，非机械套章节号。
2. "永久禁 `db push`"较既有 LEDGER 更严——采纳为**生产环境**禁令，与 Expand-Contract 分级兼容（破坏性三阶段 / 加性单迁移文件）。

### 8.3 交付
- 宪法增补：`docs/AILOS_CONSTITUTION.md` +70 行，提交 `27ef7a5..734c21f` 已推 origin/main。新增：前置阅读铁律 / Chapter 9 双语言写链路审计+onboarding 强制跳转 / Chapter 10 缓存+存量抽检 / Appendix C 生产运维(Expand-Contract+禁 db push+deploy.sh 五步+双目录同步+备份演练) / Appendix D 配置权限治理 / Appendix E 增补 OCR 成本 / Appendix F 测试验收 / 验收一票否决 5 条。
- 账簿：`AILOS_总账账簿.md` 新增 6.7（含宪法阅读确认回执）。

### 8.4 宪法阅读确认回执
```
- 读取版本：main 最新 docs/AILOS_CONSTITUTION.md（增补后 734c21f；基线 27ef7a5）
- 涉及章节：前置阅读铁律 / Ch9 双语言写链路 / Ch10 整改条款 / Appendix C·D·E·F / 验收一票否决
- 冲突校验：校准 2 处失实前提后无冲突
- 确认人：AILOS 监理（Local Agent）
```

---

## 九、宪法 734c21f 生产部署闭环（2026-07-26 18:51）

- **部署方式**：仓库标准 `deploy/deploy.sh` 七步全绿（备份 467KB → 拉取 734c21f → generate+migrate deploy → pm2 online → 前端同步 → nginx reload → health 200）；SSH 仅作调用通道，凭据不入仓。
- **双目录同步**：后端 `/www/xuewaiyu-backend`=734c21f；前端 `/www/xuewaiyu` `common.js?v=20260726a` 上线，`cache-control: no-cache` 生效。
- **多端预览**：5 核心页面 × PC+移动端 10 张截图全 200、`lc_controls=0`（证据 `_deploy_verify/`+manifest）。
- **篡改核验**：`/api/ai/chat` 注入 `targetLang=fr` 等伪造参数 → 输出仍为日语（用户库配置），前端传参无效。
- **回执**：见账簿 6.8.3（后端 commit / 静态版本 / 健康 / 预览 / 归档 五项全 ✅）。

---

## 十、Stage 11 子模块1 验收 + 4 项高优先级补正闭环（2026-07-26）

### 10.1 子模块1（静态拍照翻译）验收结论 🟢
- 链路：上传→`/api/translate/photo`→contextResolver(读 DB 语言，忽略前端)→OCR 预留→`aiGateway.chatWithMessages(scene:'photo_translate', skipAsset:true)`→解析 JSON→预留结算→返回 {原文/译文/母语解释/词汇/句型/配额}。
- 真实验收（生产 token）：translate 200（正确 JA 原文 + ZH 解释，4 词/2 句），配额 50→48，`addToNotebook`→`/api/reviews/due` 可见；注入 `targetLang=fr` 篡改无效（输出仍按用户库 ja）。
- 一票否决 5 条全部满足（详见账簿 6.9.5）。

### 10.2 4 项高优先级补正
| # | 补正项 | 结论 | 证据 |
| --- | --- | --- | --- |
| 1 | onboarding 绕过校验 | ✅ `common.js` `enforceOnboarding()` + 18 页版本 `v=20260726b` | 代码已落地 |
| 2 | 短信注册主链路 | ✅ 走通「取码→注册→选语言→进入系统」；用户 `bee326da-…`(13900000001) | `_sms_accept2_log.txt` |
| 3 | 限流白名单环境变量 | ✅ `pm2 set`+`restart --update-env`，worker `/proc/environ` 已含 `RATE_LIMIT_WHITELIST=13480010005` | `_wl_definitive_log.txt` |
| 4 | 管理员配置留痕 | ✅ 测试号 13900000001 + 白名单 13480010005 入账簿 6.9.4 | 账簿 |

### 10.3 注明缺陷（不改认证逻辑，红线）
- `send-code` 在腾讯云 SMS 上游失败时返回 **500**（非优雅 4xx）；核心「验证码落库」不受影响。建议生产监控 SMS 成功率 + 前端降级提示。属外部依赖，不改动认证/membership 逻辑。
- `pm2 set` 已固化白名单；后续 `pm2 delete` 重建需重设，建议在 `deploy/deploy.sh` step4 增加幂等 `pm2 set` 行（待推仓生效）。

### 10.4 下一步
- 子模块1 验收通过 → **进入子模块2：按量/日/周/月计费校验、时长扣减全链路**。

---

## 十一、紧急处置：全链路部署一致性核验与根因修复（2026-07-26 晚）

### 11.1 核验结论
- 双目录一致，无虚假交付：后端 HEAD=537e280；前端公网 `?v=20260726b` + `Cache-Control: no-cache`；**双目录逐文件比对 COMPARED=33, MISMATCH=0**。
- 但发现 2 个真实功能缺陷（语言切换链路），已修复。

### 11.2 根因与修复
| 根因 | 现象 | 修复 | commit |
| --- | --- | --- | --- |
| `setLanguage` 未停用旧 active 语言 | 改目标语言后 AI 输出不切换（resolver 命中旧 ja） | 先 `updateMany` 停用其余 active 再 upsert | 6bbb6b0 |
| `languageGuard` en 目标语误杀 | 切 en 后 chat 422 LANG_OUTPUT_MISMATCH | en 改为「含拉丁字母即合规」 | eb62baa |

### 11.3 部署与宪法固化
- `deploy.sh` step 5.5 双目录一致性校验（不一致即 `exit 1`+告警）；step4 幂等注入 `RATE_LIMIT_WHITELIST`。commit 4b03381。
- 宪法附录 C.5「生产部署验收三原则」（健康/同步/实测，缺一不可）。commit 9d52aaf。

### 11.4 复测
- 全局语言：set en → chat 输出英文（200）；set ja → 恢复。
- 语言控件：chat/home/learn/photo 渲染 DOM `lang-context`=0、`select`=0（profile 合法 3 select）。
- onboarding：未配置用户访问 chat → 重定向 onboarding.html；配置后正常。
- 新功能：profile「我的套餐/翻译时长/OCR」可见。

### 11.5 待办
- 服务器本地 main=9d52aaf，未推送 GitHub（无凭证）；推送前勿在生产跑 `deploy.sh`（会回滚至 537e280）。 ✅）。
