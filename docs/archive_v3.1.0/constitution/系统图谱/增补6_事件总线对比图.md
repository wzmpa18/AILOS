# 增补 6：事件总线现状-目标对比图

> 文档性质：P0 强制增补项，标注所有跨模块直接调用点，全部纳入违宪整改台账
> 关联宪法：CoreOS 第 2 章 EventBus、第 0.2 铁律 DDD

---

## 6.1 现状：跨模块直接调用链路

```mermaid
graph LR
    subgraph CURRENT["现状（违规直接调用）"]
        PRACTICE["practiceService<br/>练习完成"] -->|"⚠️ 直接调用"| POINTS["积分系统<br/>加积分"]
        PRACTICE -->|"⚠️ 直接调用"| REVIEW["复习系统<br/>生成错题"]
        CHECKIN["checkinService<br/>打卡完成"] -->|"⚠️ 直接调用"| POINTS
        PLACEMENT["placementService<br/>测试完成"] -->|"⚠️ 直接调用"| PLAN["学习计划<br/>生成计划"]
        SOCIAL["socialService<br/>好友添加"] -->|"⚠️ 直接调用"| NOTIFY["通知系统<br/>推送消息"]
    end

    style PRACTICE fill:#ffaa00
    style POINTS fill:#ffaa00
```

### 现状违规直接调用清单

| # | 调用方 | 被调用方 | 调用方式 | 违宪等级 | 整改阶段 |
|---|--------|---------|---------|---------|---------|
| 1 | practiceService | 积分系统 | 直接调用 addScore | 🟡 二级 | P2 |
| 2 | practiceService | 复习系统 | 直接调用 createReview | 🟡 二级 | P2 |
| 3 | checkinService | 积分系统 | 直接调用 addScore | 🟡 二级 | P2 |
| 4 | placementService | 学习计划 | 直接调用 generatePlan | 🟡 二级 | P2 |
| 5 | socialService | 通知系统 | 直接调用 sendNotification | 🟡 二级 | P2 |

## 6.2 目标：事件总线发布-订阅模式

```mermaid
graph LR
    subgraph TARGET["目标（事件总线解耦）"]
        PRACTICE2["practiceService"] -->|"发布 LearningCompleted"| BUS["CoreOS EventBus"]
        CHECKIN2["checkinService"] -->|"发布 CheckinCompleted"| BUS
        PLACEMENT2["placementService"] -->|"发布 PlacementCompleted"| BUS
        SOCIAL2["socialService"] -->|"发布 FriendAdded"| BUS

        BUS -->|"订阅"| POINTS2["积分系统<br/>自动加分"]
        BUS -->|"订阅"| REVIEW2["复习系统<br/>自动生成错题"]
        BUS -->|"订阅"| PLAN2["学习计划<br/>自动生成计划"]
        BUS -->|"订阅"| NOTIFY2["通知系统<br/>自动推送"]
        BUS -->|"订阅"| AUDIT["审计日志<br/>自动归档"]
    end

    style BUS fill:#4F46E5,color:#fff
```

## 6.3 整改路线

```
现状：5 处直接调用 → P2 EventBus 落地 → 全部改为发布-订阅 → P3 核验 0 直接调用 → FROZEN
```

---

> **增补 6 版本**: v1.0 | **归档路径**: AILOS_指令中心/系统图谱/增补6_事件总线对比图.md
