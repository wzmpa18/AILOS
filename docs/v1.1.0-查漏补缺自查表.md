# AILOS v1.1.0 存量保底查漏补缺 · 自查完成表

- **交付日期**：2026-08-03
- **交付性质**：存量保底整改（增量补全 / Bug 修复 / 规范对齐）
- **基线提交**：`16254a8`
- **自测结论**：后端 20/20 PASS，前端静态 0 问题（16 个脚本块），本地 HTTP 33/33 PASS，存量功能丢失 **0 项**

---

## 零、总纲三条红线自查

| 红线 | 自查结论 | 证据 |
|------|----------|------|
| 禁止删除任何现有功能入口 | ✅ 未删除任何入口，仅新增 2 个页面、补齐 3 项入口 | `scripts/verify-no-regression.js` 输出「0 项丢失」 |
| 禁止废弃已实现的业务逻辑 | ✅ 游戏三件套、拍照翻译、练习题型组件全部复用回迁，未推倒重来 | 同上，逐函数比对 HEAD 与工作区 |
| 禁止跳过自检直接提交 | ✅ 三套自测脚本全部通过后才提交 | `selftest-v110.js` / `check-html-js.js` / `selftest-http.js` |

> **本次唯一的"删除"操作说明**（按红线要求单独标注）：
> 1. `profile.html` 主设置列表中的「注销账号」条目 —— 属于宪法 一-1 明确要求的**入口迁移**，已完整迁移至新建的 `account-security.html` 页面底部，注销逻辑本身一行未改。
> 2. `game.html` / `learn.html` / `practice.html` / `translate.html` 中**硬编码的底部导航 HTML** —— 属于宪法 六-1 要求的**统一为组件注入**，导航项由 4~5 项补齐为 7 项，是功能增强而非删减。
> 3. 各页面对 `tokens.css` 的引用 —— 该文件在仓库与生产**均不存在**，属于无效引用，移除后减少无谓 404。

---

## 一、个人中心与账号体系（4 项）

| # | 核验项 | 状态 | 处理说明 | 验收证据 |
|---|--------|------|----------|----------|
| 1 | 注销账号入口层级 | ✅ 已完成 | **发现严重回归**：上一版已从主页移除注销入口，但承诺的「账号安全页」从未创建，导致注销功能**完全不可达**。本次新建 `account-security.html`，页面最底部以灰色小字展示，点击弹出二次确认（需手动输入 `DELETE`），并在 profile 补回「账号安全」入口。原注销接口 `DELETE /api/user/me` 未改动。 | `account-security.html:120-135`；自查脚本 `账号安全页存在` PASS |
| 2 | 登录态全局不丢失 | ✅ 已完成 | 全站 token 读写收口到 `common.js`：新增 `setToken/clearToken/authFetch`，主键 `yandao_token_v1`，兼容并自动回迁 `auth_tokens/ailos_token/token/accessToken` 四种历史键名。`learn.html` 原先仅在无 token 时跳登录、接口异常也可能误判，现改为**仅 401 判定失效**，其余错误保留登录态并给出重试。 | `common.js:145-215`；HTTP 自测 `含 clearToken/setToken 收口` PASS |
| 3 | 双语言设置全局生效 | ✅ 已完成 | **修复硬编码 Bug**：`learn.html` 原写死 `language=ja`，日语以外用户内容串语；`game.html` 原词库写死英文单词 + TTS 写死 `en-US`。现全部改为读取 `AILOS.getStudyLang()`，并监听 `languageChanged` 事件热更新。游戏新增日/韩/英三套词库，TTS 按语种映射。 | `learn.html:78-95`、`game.html:186-260`；自查 `learn 无硬编码 language=ja` PASS；后端 4 项语言一致性用例 PASS |
| 4 | 语言入口唯一性 | ✅ 无需修改 | 全仓库扫描确认：语言切换控件仅存在于 `profile.html`（个人中心设置）与 `onboarding.html`（首次引导），其余页面无独立切换控件。`translate.html` 新增语言提示条，明确引导跳转个人中心修改。 | 全量搜索 `setStudyLang/语言切换/changeLanguage` 结果 |

---

## 二、关于我们与合规页面（4 项）

| # | 核验项 | 状态 | 处理说明 | 验收证据 |
|---|--------|------|----------|----------|
| 1 | 会员服务链接修复 | ✅ 已完成（本次校验保持） | 链接为站内 `/xuewaiyu/membership.html`，无任何公司官网外跳。 | 自查 `about 会员链接站内` + `无外部公司官网跳转` PASS；HTTP 探测 200 |
| 2 | 用户服务协议修复 | ✅ 已完成（本次校验保持） | 指向 `/xuewaiyu/terms.html`，实测 200，页面返回按钮可用。 | HTTP 自测 `about 链接可达 /xuewaiyu/terms.html` PASS |
| 3 | 隐私政策校验 | ✅ 已完成（本次校验保持） | 指向 `/xuewaiyu/privacy.html`，实测 200，零 404。 | HTTP 自测 PASS |
| 4 | 意见反馈功能补齐 | ✅ 已完成 | **原为 `mailto:` 跳空**。本次新建完整闭环：<br>① 前端表单 `feedback.html`：问题类型（6 类）+ 问题描述（≤2000 字）+ 联系方式 + 截图上传（≤3 张，≤5MB）<br>② 后端新增 `POST /api/feedback`，记录落盘留存（`data/feedback/`），并通过内置零依赖 SMTP 客户端发送邮件至 **wuzhimin666@163.com**；未配 SMTP 时降级为「留存 + 待发队列」，**绝不因邮件失败导致用户提交失败**<br>③ 提交成功返回工单号 + 友好中文提示 | `feedback.html`、`src/server/routes/feedback.js`、`src/server/services/feedbackService.js`；后端自测 6 项 PASS（含工单号生成、落盘、降级） |

> **邮件发送说明**：服务器需配置 `SMTP_HOST / SMTP_USER / SMTP_PASS` 三个环境变量后邮件才会真实投递。未配置时反馈**不会丢失**，全部留存在 `data/feedback/feedback.jsonl`，可通过 `GET /api/feedback/list` 查看。

---

## 三、翻译引擎模块（3 项）

| # | 核验项 | 状态 | 处理说明 | 验收证据 |
|---|--------|------|----------|----------|
| 1 | 拍照翻译功能补全 | ✅ 已完成 | **发现接口错配 Bug**：前端调用 `/api/translate/ocr`（该路由**不存在**），而后端实际提供的是 `POST /api/translate/photo`，参数为 `{imageBase64, mimeType}` —— 即拍照翻译在生产**从未成功过**。本次：① 新增「直接拍照」按钮（`capture="environment"` 调起摄像头），保留原相册选择；② 对齐真实后端接口；③ 复用后端已实现的 `words[]` 返回，实现**生词一键收藏**（`POST /api/translate/notebook`）。OCR 逻辑为后端既有实现，未重写。 | `translate.html:230-300`；后端 `routes/translate.js` 既有 `/photo`、`/notebook` |
| 2 | 实时扫描 / 对话翻译入口保留 + 状态补齐 | ✅ 已完成 | 改为三 Tab 结构，**三大入口永久可见**，无「即将上线」字样。两个功能均展示「终身 5 分钟免费体验」徽章 + 套餐入口，并对接后端既有 `GET /api/translate/trial/status` 展示真实剩余时长；点击「开始体验」跳转生产已上线的 `scan-translate.html` / `conversation-translate.html`（实测均 200）。计费复用现有配额体系。 | `translate.html:150-200`；HTTP 自测两页面 200 PASS；全站无「即将上线」PASS |
| 3 | 语言匹配校验 | ✅ 已完成 | 移除硬编码 `ja`，目标语言取自 `AILOS.getStudyLang()`，页面顶部实时显示当前目标语言并提供个人中心跳转，监听 `languageChanged` 热更新。 | `translate.html:305-320` |

---

## 四、句型练习模块（3 项）

| # | 核验项 | 状态 | 处理说明 | 验收证据 |
|---|--------|------|----------|----------|
| 1 | 自定义时长生效 | ✅ 已完成 | **修复 3 处真实 Bug**：<br>① 前端请求**未携带 duration 参数**，后端只读数据库旧配置 → 改为透传 `&duration=`；<br>② 后端 `calcSentenceCount` 写成 `durationMinutes / rate`，与注释语义相反，导致 30 分钟算出 60 题、且**高级(100题)比初级(60题)题目更多**（难度越高题越多，逻辑颠倒）→ 修正为 `minutes * rate`；<br>③ 兜底句型数量写死 5 条，不随时长变化 → 改为按 `totalCount` 生成，句库不足时循环补足并标注复习轮次。<br>另新增本地持久化，接口慢/失败也能保留上次选择。 | 后端自测：10min=5题 / 20min=10题 / 30min=15题 / 自定义15min=8题，且 `20>10`、`30>20`、`高级≤初级` 全部 PASS |
| 2 | 题型分级优化 | ✅ 已完成 | 原逻辑 `currentIdx % 3` 无差别轮换三种题型，初级用户也会遇到填空和跟读。现按难度控制展示：**初级全部四选一选择题**，中级选择题为主+填空，高级三种轮换。**原有题型组件全部保留**，仅做展示控制。<br>另修复：题目数少时干扰项不足会退化成二/三选一，现补充通用干扰项**确保恒为四选一**。 | `practice.html:449-462`、`practice.html:505-520` |
| 3 | 发音功能加固 | ✅ 已完成 | 修复偶发无声的两个真实主因：① 未取消上一条语音导致排队卡死 → 每次先 `cancel()`；② `getVoices()` 异步加载，首次调用常返回空数组 → 监听 `voiceschanged` + 350ms 超时兜底。另新增按语种精确匹配发音人、APP 内优先走原生 TTS 桥接、失败时给出中文提示。 | `practice.html:TTS 段`；`game.html` 同步加固 |

---

## 五、游戏模块（3 项）

| # | 核验项 | 状态 | 处理说明 | 验收证据 |
|---|--------|------|----------|----------|
| 1 | 单词闯关完整闭环 | ✅ 已完成 | 完整链路已具备：开始→出题→四选一→正误反馈→10 题结算→星级评分。本次补强：① 题目**匹配用户目标语言**（原写死英文词库）；② 新增**错题回顾**列表（原缺失）；③ 修复选项点击 Bug（原将词条内容拼进 `onclick` 字符串，遇含撇号词条会截断 HTML 属性导致点击报错），改为 DOM 事件绑定。 | `game.html:290-360`；语法校验 PASS |
| 2 | 拼写测试完整实装 | ✅ 已完成 | 无「即将上线」，可进入、可输入、可结算，错误时显示正确答案。本次补强：日/韩语种下键盘无法输入汉字/假名/谚文，**实际不可玩** → 现接受罗马音作答（如 `ringo`），并做长音符归一化（`kōri`≈`kori`），输入框动态提示。新增错题回顾。 | `game.html:465-505` |
| 3 | 听力挑战完整实装 | ✅ 已完成 | 播放发音→四选一→正误反馈→10 题结算，支持重复播放。本次补强：TTS 按目标语言发音（原固定英文，日语单词用英文音色读不出来）；同步修复 `onclick` 引号截断问题；新增错题回顾。 | `game.html:510-560` |

---

## 六、全局基础体验（2 项）

| # | 核验项 | 状态 | 处理说明 | 验收证据 |
|---|--------|------|----------|----------|
| 1 | 底部导航全页面统一 | ✅ 已完成 | **发现最严重的一处基建 Bug**（详见下方"重大发现"）：`deploy.sh` 只同步 `public/xuewaiyu/*`，**`public/assets/` 从未部署**，导致生产 `/assets/common.js` 恒 404 —— 而所有页面都用这个路径引导航组件，即**全站底部导航组件从未真正生效**。<br>修复：① 全站引用改为生产实际可用的 `/xuewaiyu/assets/`；② `deploy.sh` 补充同步 `assets` 与根级页面，并加入部署门禁；③ `game/learn/practice/translate` 四页移除 4~5 项硬编码导航，统一由组件注入 7 项。 | HTTP 自测：7 项导航目标页**全部 200**；8 个一级页面 `nav+common.js` 全 PASS |
| 2 | 空状态兜底补齐 | ✅ 已完成 | `learn.html` 加载失败改为友好中文提示 + **重新加载按钮**；`translate.html` 体验时长加载失败给出重试按钮，识别失败给出中文原因；`feedback.html` 类型枚举接口失败时用本地兜底不空屏；后端反馈接口任何异常统一返回中文提示，杜绝英文报错。 | `learn.html:showLoadError()`、`translate.html:loadTrial()`、`feedback.js` |

---

## 七、本次重大发现（此前从未被发现的存量隐患）

| 编号 | 问题 | 影响 | 修复 |
|------|------|------|------|
| **F-01** | `deploy.sh` 从未同步 `public/assets/`，生产 `/assets/common.js` 恒 404 | **全站底部导航组件、登录态统一逻辑从未生效**，各页面只能靠自己的硬编码导航 | 修正部署脚本 + 全站路径统一 + 部署门禁校验 |
| **F-02** | 注销账号入口被移除，但目标页从未创建 | **注销功能完全不可达**，属合规风险 | 新建 `account-security.html` |
| **F-03** | 前端调 `/api/translate/ocr`，后端实为 `/api/translate/photo` | **拍照翻译在生产从未成功过** | 对齐真实接口 |
| **F-04** | `calcSentenceCount` 乘除写反 | 30 分钟出 60 题；**高级比初级题目更多**，难度逻辑颠倒 | 修正换算 |
| **F-05** | `review.html`、`ai-companion-builder.html` 生产已上线但**不在仓库** | 一旦用 `rsync --delete` 部署将**永久删除两个已上线页面** | 已从生产回迁纳入版本控制 |
| **F-06** | 游戏选项把词条拼进 `onclick` 字符串 | 含撇号词条（如 `don't`）点击即报错无法作答 | 改 DOM 绑定 |

---

## 八、自测执行记录

```
$ node scripts/selftest-v110.js
[模块四-1] 句型练习自定义时长生效     4 PASS
[模块四-1] 兜底句型数量匹配时长       3 PASS
[模块二-4] 意见反馈：留存 + 邮件目标  6 PASS
[模块一-3] 语言一致性                 4 PASS
[全局]     路由加载完整性             1 PASS  (24 route modules loaded)
===== 自测结果：20 通过 / 0 失败 =====

$ node scripts/check-html-js.js
内联 JS 语法校验：16 个脚本块全部 OK
v1.1.0 静态规范检查：全部 PASS
===== 结果：0 个问题 =====

$ node scripts/selftest-http.js   （本地 HTTP 模拟生产 docroot）
[六-1] 底部导航 7 项目标页        7 PASS
[全局] 核心页面可达性            11 PASS
[一-2] common.js 收口             4 PASS
[六-2] 页面内静态引用零 404       6 PASS
[二]   关于我们页链接零 404       5 PASS
===== HTTP 自测：33 通过 / 0 失败 =====

$ node scripts/verify-no-regression.js
===== 存量功能核验：0 项丢失 =====
```

---

## 九、待人工验证项（需真机 / 生产环境）

以下项目本地已完成代码自测，但需部署后人工补充截图证据：

1. 生产部署后回归 14 个核心页面截图（带地址栏）
2. Console 零报错、Network 零 404 截图
3. 三个游戏各完整玩一轮的结算截图
4. 反馈表单真实提交 + 邮箱收信截图（**需先配置 SMTP 环境变量**）
5. 安卓 APK 真机验证：录音授权、TTS 发音、返回键、登录持久化

---

## 十、部署注意事项

1. **必须使用本次修正后的 `deploy.sh`**，否则 `assets` 仍不会同步，导航修复不生效。
2. 部署后建议执行 `node scripts/probe-prod.js` 做线上冒烟。
3. 反馈邮件需在服务器 `.env.production` 增加：
   ```
   SMTP_HOST=smtp.163.com
   SMTP_PORT=465
   SMTP_USER=<发信邮箱>
   SMTP_PASS=<授权码>
   FEEDBACK_EMAIL=wuzhimin666@163.com
   ```
4. 反馈数据目录 `data/feedback/` 需可写，建议纳入备份。
