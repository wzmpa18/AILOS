# AILOS v3.3.0 迭代规划建议

> **文档性质**: v3.3.0 迭代规划建议（基于 v3.2.0 封版基线）
> **规划版本**: v3.3.0
> **上一版本基线**: v3.2.0-release（Git 基线 `edb1537`，标签 `v3.2.0-release`）
> **编制日期**: 2026-08-07
> **适用对象**: 总工程师、开发团队、运维团队、监理端
> **编制依据**: 终验审计报告 v3.2.0 + AILOS 移交说明书 v3.2.0 + 双宪法 v3.2.0 终版 + 架构蓝图 v3.2.0 终版

---

## 一、v3.2.0 版本基线回顾

### 1.1 交付内容概述

v3.2.0 为「社交板块升级」版本，在 v3.1.0 基线上完成「站内优质动态 + 站外外语资讯聚合」双模块交付，核心内容如下：

| 模块 | 交付内容 | 审计结论 |
|------|---------|---------|
| 站内优质动态 | SocialTimeline 新增 5 字段（tag/isQuality/qualityMarkedBy/qualityMarkedAt/favoriteCount）；优质推荐排序（isQuality→likeCount→favoriteCount→commentCount→createdAt）；标签筛选体系（experience/study_abroad/exam_prep/find_partner）；加精标识机制 | 🟢 通过 |
| 站外外语资讯 | 4 个独立数据表（NewsSource/NewsArticle/NewsReport/NewsAuditLog）；三层去广告过滤（关键词规则+AI辅助+白名单）；定时增量抓取（cron 06:00/18:00）；审核闭环（pending→approved/rejected）；举报处理；审计日志全量记录 | 🟢 通过 |
| 后台资讯管理 | 来源管理（新增/删除/拉黑/恢复）；内容审核（approve/reject）；一键下架；举报处理；全局 AI 开关（默认关闭）；手动抓取；审计日志查看 | 🟢 通过 |

### 1.2 审计结论

- **13 项红线全部通过**（其中 6 项经整改闭环：用户协议免责条款、原作者显示、AI调用审计、抓取退避重试、全局AI开关、评论关注功能记录为后续迭代）
- **零一级违宪，零功能断层，零数据丢失**
- **综合评级：🟢 低风险，v3.2.0 社交板块升级可发布**

### 1.3 Git 基线信息

| 项 | 值 |
|----|-----|
| 当前版本 Git 基线 | `edb1537` |
| 版本标签 | `v3.2.0-release` |
| 分支 | main |
| 关联文档 | 双宪法 v3.2.0 终版 / 架构蓝图 v3.2.0 终版 / 终验审计报告 v3.2.0 / 移交说明书 v3.2.0 |

> v3.3.0 所有迭代工作均在 `edb1537` 基线之上展开，禁止对冻结层做任何回溯性修改。

---

## 二、v3.3.0 迭代优先级

按用户指令，v3.3.0 迭代优先级排序为：

```
P0 支付接入 → P1 佣金结算 → P2 口语评测 → P3 内容厚度扩充
```

> 说明：P0/P1/P2/P3 为迭代批次与重要性优先级，并非严格的串行阻塞关系。P1 佣金结算强依赖 P0 支付订单；P2 口语评测与 P3 内容厚度扩充可在 P0 推进期间并行启动设计与素材准备。

### P0：支付接入

#### 功能概述

接入微信支付与支付宝支付能力，构建完整的「下单 → 拉起支付 → 异步回调 → 订单状态变更」链路，打通平台商业化变现闭环，并与既有会员体系对接，支撑 VIP 订阅、课程购买、增值服务购买等场景。

#### 涉及模块和数据表

| 类别 | 内容 |
|------|------|
| 新增功能域 | 支付域（payment） |
| 新增数据表 | `Order`（订单主表）、`PaymentRecord`（支付流水）、`PaymentConfig`（支付渠道配置）、`RefundRecord`（退款记录，预留） |
| 既有数据表（仅新增字段） | `User`（绑定支付账户标识）、`MemberSubscription`（关联订单号） |
| 新增服务层 | `src/services/paymentService.js`（下单/查单/回调处理）、`src/services/paymentCallbackService.js`（回调验签与状态机推进） |
| 新增路由层 | `src/server/routes/payment.js`（客户端：创建订单/查询订单/拉起支付）、`src/server/routes/adminPayment.js`（管理端：订单查询/对账/退款/审计） |
| 新增前端页面 | 支付收银台页面、会员订购页、后台订单管理页 |
| 配置文件 | `.env.example` 新增 `WECHAT_PAY_*` / `ALIPAY_*` / `PAYMENT_NOTIFY_URL` 等 |

#### 预估工作量

| 项 | 评级 | 说明 |
|----|------|------|
| 工作量 | **P0（高）** | 商业化核心能力，涉及资金链路，需严格的安全设计与测试 |
| 预估人日 | 12~16 人日 | 含 SDK 集成、订单表设计、回调处理、会员对接、对账、测试 |

#### 依赖关系

- 依赖现有会员体系（`User` / `MemberSubscription`），支付成功后回写会员等级与有效期
- 依赖线上 HTTPS 环境与公网可达的回调地址
- **P1 佣金结算强依赖本模块**：佣金计算基于 `Order` 的已支付订单
- 依赖审计日志体系（支付/退款操作必须落审计）

#### 风险点

| 风险 | 等级 | 应对 |
|------|------|------|
| 回调验签失败/伪造回调 | 🔴 高 | 严格验签 + 金额一致性校验 + 幂等控制 + 防重放 |
| 订单状态不一致（掉单） | 🔴 高 | 主动查单补偿任务 + 状态机强约束 + 回调与查单双通道对账 |
| 商户资质与合规（ICP/商户号） | 🟡 中 | 提前申请微信商户号、支付宝应用，完成资质审核 |
| 第三方 SDK 版本升级/接口变更 | 🟡 中 | 锁定 SDK 版本，封装 adapter 层隔离变更 |
| 退款链路资金安全 | 🟡 中 | 退款独立审核流程 + 双人复核 + 审计日志 |

---

### P1：佣金结算

#### 功能概述

基于 P0 支付订单构建分销佣金体系，支持分销关系绑定、多级佣金规则配置、佣金计算与入账、提现申请与审核流程、佣金报表统计，形成「推广 → 成交 → 分佣 → 提现」闭环。

#### 涉及模块和数据表

| 类别 | 内容 |
|------|------|
| 新增功能域 | 佣金域（commission） |
| 新增数据表 | `DistributionRelation`（分销关系）、`CommissionRule`（佣金规则）、`CommissionRecord`（佣金入账记录）、`WithdrawRequest`（提现申请）、`WithdrawAuditLog`（提现审计） |
| 既有数据表（依赖） | `Order`（已支付订单触发佣金计算）、`User`（分销人/提现人） |
| 新增服务层 | `src/services/commissionService.js`（佣金计算/入账）、`src/services/withdrawService.js`（提现申请/审核/打款） |
| 新增路由层 | `src/server/routes/commission.js`（客户端：我的佣金/提现申请）、`src/server/routes/adminCommission.js`（管理端：规则配置/提现审核/报表） |
| 新增前端页面 | 我的佣金页、提现申请页、后台佣金报表与审核页 |

#### 预估工作量

| 项 | 评级 | 说明 |
|----|------|------|
| 工作量 | **P1（中高）** | 资金相关，涉及分销规则与提现审核，需严谨的状态机 |
| 预估人日 | 10~14 人日 | 含分销关系、佣金计算、提现审核、报表、测试 |

#### 依赖关系

- **强依赖 P0 支付接入**：佣金计算以 `Order` 已支付订单为唯一触发源
- 依赖用户体系（分销人身份、提现账户）
- 提现打款可对接支付域的商户付款能力（企业付款到零钱/转账）

#### 风险点

| 风险 | 等级 | 应对 |
|------|------|------|
| 多级分销涉传销法律风险 | 🔴 高 | 分销层级严格限制（建议≤2级），佣金比例合规设计，法务评审 |
| 提现刷单/套利 | 🔴 高 | 提现审核流程 + 风控规则（最小提现额/频率限制/实名校验） |
| 佣金计算并发一致性 | 🟡 中 | 订单维度幂等 + 事务保障 + 佣金入账唯一约束 |
| 提现打款失败/资金对账 | 🟡 中 | 打款状态机 + 失败重试 + 人工复核 + 对账报表 |

---

### P2：口语评测

#### 功能概述

接入 AI 口语评分能力，支持录音上传与存储、AI 评分（发音/流利度/完整度多维评分）、评分结果展示，丰富学习核心域的口语训练能力。

> **关键约束**：AI 评分能力必须统一走 `BrainFacade → aiService → adapters` 调用链，业务代码零直连大模型；AI 调用必须纳入 `aiQuotaService` 配额管控。

#### 涉及模块和数据表

| 类别 | 内容 |
|------|------|
| 新增功能域 | 口语评测域（oral）— 归入学习核心域扩展，不跨域耦合 |
| 新增数据表 | `OralTask`（评测任务/题目）、`OralRecord`（录音记录）、`OralScore`（评分结果） |
| 既有数据表（依赖） | `User`（学习者）、学习记录表（评测成绩归档） |
| 新增服务层 | `src/services/oralService.js`（任务管理/录音存储/评分调度） |
| 调度层调用 | 通过 `BrainFacade` 调度口语评分 AI 能力（**禁止修改 src/brain/ 调度核心**） |
| 新增路由层 | `src/server/routes/oral.js`（客户端：领取任务/上传录音/查询评分） |
| 存储 | 录音文件存储（对象存储或本地受控目录），数据库仅存路径与元数据 |
| 新增前端页面 | 口语训练页、评分结果展示页 |

#### 预估工作量

| 项 | 评级 | 说明 |
|----|------|------|
| 工作量 | **P2（中）** | AI 走既有调度链，技术路径清晰；录音存储与评分展示为常规开发 |
| 预估人日 | 8~12 人日 | 含任务设计、录音上传、BrainFacade对接、评分展示、配额接入、测试 |

#### 依赖关系

- 依赖 `BrainFacade` 调度层（一级冻结层，**仅调用不修改**）
- 依赖 `aiQuotaService` 配额管控（口语评分消耗 AI 额度，需独立计量）
- 依赖录音文件存储能力（对象存储/受控本地目录）
- 依赖审计日志体系（AI 调用必须落审计）

#### 风险点

| 风险 | 等级 | 应对 |
|------|------|------|
| AI 成本失控 | 🟡 中 | 纳入 aiQuotaService 配额管控，单任务限评 1 次，默认可设每日上限 |
| 架构合规（直连大模型） | 🔴 高 | 强制走 BrainFacade，代码审计禁止 require 模型 SDK / axios 直连网关 |
| 录音存储成本 | 🟡 中 | 录音压缩 + 保留期策略 + 冷热分层存储 |
| AI 评分波动/稳定性 | 🟡 中 | 评分结果缓存 + 异常兜底（评分失败可重试，限次） |

---

### P3：内容厚度扩充

#### 功能概述

扩充平台内容素材厚度，涵盖词汇库扩充、题库扩充、资讯来源扩充三个方向，提升平台内容供给与学习价值。本项以内容运营为主、技术开发为辅。

#### 涉及模块和数据表

| 类别 | 内容 |
|------|------|
| 词汇库扩充 | 既有 `Vocabulary` 表数据扩充，通过 `prisma/seed` 脚本批量导入，扩充各语种/各等级词汇量 |
| 题库扩充 | 既有题库表数据扩充，补充听力/阅读/语法/词汇等题型题量 |
| 资讯来源扩充 | 既有 `NewsSource` 表扩充受控白名单来源（权威资讯源/教育平台/官方媒体），通过后台来源管理录入 |
| 涉及模块 | 词汇 seed 脚本、题库导入工具、后台资讯来源管理（既有能力） |

#### 预估工作量

| 项 | 评级 | 说明 |
|----|------|------|
| 工作量 | **P3（低-中）** | 以内容运营为主，技术侧为 seed 脚本与导入工具维护 |
| 预估人日 | 5~8 人日 | 含词汇/题库数据准备与校验、来源录入与抓取验证 |

#### 依赖关系

- 词汇库扩充依赖既有 `prisma/seed` 机制与 `vocabulary seed` 数据导入流程
- 题库扩充依赖既有题库导入工具
- 资讯来源扩充依赖既有 `NewsSource` 后台管理与 `newsCrawlJob` 抓取链路（**禁止修改抓取核心逻辑**，仅扩充来源数据）

#### 风险点

| 风险 | 等级 | 应对 |
|------|------|------|
| 题库/词汇版权 | 🟡 中 | 仅采用自有/授权/公开授权内容，版权审查 |
| 内容准确性 | 🟡 中 | 导入前人工抽检 + 校对流程 |
| 资讯来源合规 | 🟡 中 | 仅录入权威白名单来源，禁止抓取无版权内容，符合 v3.2.0 知识产权合规要求 |
| 抓取配额压力 | 🟢 低 | 来源扩充受 `dailyLimit` 与 cron 频次双重管控，自动限流 |

---

### 优先级与工作量汇总

| 优先级 | 功能项 | 预估人日 | 关键依赖 | 是否阻塞后续 |
|--------|--------|---------|---------|------------|
| P0 | 支付接入 | 12~16 | 会员体系、HTTPS、商户资质 | 是（阻塞 P1） |
| P1 | 佣金结算 | 10~14 | P0 支付订单 | 否 |
| P2 | 口语评测 | 8~12 | BrainFacade、aiQuotaService、存储 | 否 |
| P3 | 内容厚度扩充 | 5~8 | seed 机制、来源管理 | 否 |
| **合计** | — | **35~50 人日** | — | — |

---

## 三、v3.2.0 遗留项

以下为 v3.2.0 终验审计中记录的遗留项（源自整改 6 记录及移交说明书下一版本建议），建议在 v3.3.0 迭代中择机承接，不阻塞 P0~P3 主线推进：

| # | 遗留项 | 来源 | 建议处理 |
|---|--------|------|---------|
| 1 | 站内动态评论功能 | 整改 6 记录 | SocialTimeline 评论链路完整实现，新增评论表，归入社交域扩展 |
| 2 | 站内动态关注/粉丝体系 | 整改 6 记录 | 用户关注/粉丝关系建模，动态流按关注人过滤，归入社交域扩展 |
| 3 | AI 摘要增强 | 移交说明书 P2 建议 | 开启 AI 后自动生成内容摘要，走 BrainFacade 调度，受配额管控 |
| 4 | 资讯分类打标自动化 | 移交说明书 P2 建议 | AI 辅助分类标签，走 BrainFacade 调度，受配额管控 |

> 遗留项处理原则：归入既有功能域，不跨域耦合；AI 类遗留项必须走 BrainFacade 统一调度并纳入配额管控；所有新增数据操作必须有审计日志。

---

## 四、冻结层边界声明

以下为一级冻结层，v3.3.0 迭代期间禁止任何形式的修改（含重构、字段变更、逻辑调整）：

| # | 冻结项 | 路径/范围 | 冻结说明 |
|---|--------|----------|---------|
| 1 | ContentBrain 调度核心 | `src/brain/` | AI 调度统一收口层，禁止修改调度逻辑；新增 AI 能力仅通过既有接口调用 |
| 2 | prisma 既有字段 | `prisma/schema.prisma` 既有模型与字段 | 仅允许新增模型与新增字段，禁止删除/重命名/修改既有字段类型 |
| 3 | deploy.sh 核心部署逻辑 | `deploy.sh` | 五步部署 + 四端哈希校验 + 双端冒烟测试 + 回滚机制不可修改 |
| 4 | BrainFacade→aiService→adapters 调用链 | AI 调度统一收口架构 | 禁止业务代码直连大模型（禁止 require 模型 SDK / axios 直连 AI 网关） |
| 5 | NewsAuditLog 审计日志 | `NewsAuditLog` 表与审计服务 | 审计日志保留期 ≥ 180 天，禁止物理删除，禁止关闭审计写入 |

> 冻结层解冻须经总工程师审批，并同步更新双宪法与架构蓝图增量条款。

---

## 五、可迭代范围

v3.3.0 迭代允许在以下范围内自由扩展（不触及一级冻结层）：

| # | 可迭代范围 | 说明 |
|---|-----------|------|
| 1 | 新增内容素材 | 词汇、题库、资讯来源等数据扩充（seed / 后台录入） |
| 2 | 新增业务页面 | 支付收银台、佣金报表、口语训练页等新增前端页面 |
| 3 | 前端交互优化 | 既有页面交互体验优化、响应式适配、加载性能优化 |
| 4 | 运营功能新增 | 后台运营管理功能（订单管理、佣金审核、来源管理等） |
| 5 | 新增数据表和字段 | 新增数据表（Order/Commission/OralTask 等）、既有表新增字段（不修改既有字段） |

> 新增数据表须与既有核心数据表物理隔离，避免外键强耦合；新增字段须遵循 ADD-only 原则。

---

## 六、架构合规红线

v3.3.0 所有新增功能必须遵守以下架构合规红线，违者按一级违宪处理：

| # | 红线 | 要求 |
|---|------|------|
| 1 | 所有 AI 能力必须走 ContentBrain 统一调度 | 口语评分、AI 摘要、资讯打标等 AI 调用必须经 `BrainFacade → aiService → adapters` 链路，业务代码零直连大模型（禁止 require 模型 SDK / axios 直连 AI 网关） |
| 2 | 新增功能必须纳入既有功能域，不跨域耦合 | 支付域、佣金域、口语评测域须独立路由/独立 Service/独立数据表，与既有域解耦，禁止跨域强依赖 |
| 3 | 站外内容必须标注来源，禁止全文转载 | 资讯来源扩充须遵循 v3.2.0 知识产权合规：仅展示标题+摘要（≤100字）+来源+跳转链接，禁止全文转载 |
| 4 | 所有新增 API 必须有鉴权层 | 公开 API 须限频，用户级 API 须登录态校验，管理 API 须管理员权限校验，禁止裸奔接口 |
| 5 | 所有新增数据操作必须有审计日志 | 支付/退款/佣金/提现/口语评分/资讯审核等关键操作必须落审计日志，保留期 ≥ 180 天 |

> 合规验收将延续 v3.2.0 的 13 项红线一票否决机制，v3.3.0 封版前须通过同等强度的独立合规审计。

---

## 七、版本里程碑建议

| 里程碑 | 内容 | 预估节点 |
|--------|------|---------|
| M1 设计评审 | 支付域表结构/API/状态机设计评审；冻结层边界确认 | 迭代启动后第 1 周 |
| M2 P0 支付接入封板 | 微信/支付宝 SDK 集成、订单链路、回调处理、会员对接完成 | 第 2~3 周 |
| M3 P1 佣金结算封板 | 分销关系、佣金计算、提现审核、报表完成 | 第 3~4 周 |
| M4 P2 口语评测封板 | BrainFacade 对接、录音存储、评分展示、配额接入完成 | 第 4~5 周 |
| M5 P3 内容厚度扩充 | 词汇/题库/资讯来源扩充完成 | 第 5 周（可与 M4 并行） |
| M6 遗留项承接 | v3.2.0 遗留项（评论/关注/AI摘要/打标）择机承接 | 第 5~6 周 |
| M7 终验审计 | 13 项红线独立合规审计 + 部署验证 + 封版 | 第 6 周 |

---

## 八、关联文档索引

| 文档 | 路径 |
|------|------|
| 双宪法 v3.2.0 终版 | `docs/AILOS_双宪法_v3.2.0_终版.md` |
| 架构蓝图 v3.2.0 终版 | `docs/AILOS_架构蓝图_v3.2.0_终版.md` |
| 终验审计报告 v3.2.0 | `docs/终验审计报告v3.2.0.md` |
| 移交说明书 v3.2.0 | `docs/AILOS_移交说明书_v3.2.0.md` |
| v3.3.0 迭代规划建议 | `docs/v3.3.0_迭代规划建议.md`（本文档） |

---

> **v3.3.0 迭代规划建议编制完成。本规划以 v3.2.0-release（`edb1537`）为基线，优先级为 支付接入 → 佣金结算 → 口语评测 → 内容厚度扩充，全程严守一级冻结层边界与架构合规红线，封版前须通过 13 项红线独立合规审计。**
