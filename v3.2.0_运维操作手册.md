# AILOS v3.2.0 运维操作手册

> 本手册基于 `deploy.sh`（标准化一键部署脚本 v1.2 深度穿透审计加固版）及 v3.2.0 实际代码编写，所有命令、路径、校验逻辑均来自生产真值源，未编造任何不存在的功能。
>
> 运维通道：腾讯云轻量云「执行命令」控制台
> 部署脚本：`bash deploy.sh`
> 部署方式：在项目目录 `/www/xuewaiyu-backend` 下执行

---

## 核心路径速查

| 变量 | 路径 | 说明 |
|------|------|------|
| `PROJECT_DIR` | `/www/xuewaiyu-backend` | 后端代码根目录（git 仓库） |
| `WWW_DIR` | `/www/xuewaiyu` | 前端静态资源部署目录 |
| `BACKUP_DIR` | `/www/backups` | 备份文件统一存放目录 |
| `NGINX_VHOST_DIR` | `/www/server/panel/vhost/nginx` | 宝塔面板 nginx vhost 配置目录 |
| `NGINX_CONF_SRC` | `$PROJECT_DIR/deploy/nginx` | nginx 配置 git 真值源 |
| 后端服务 | `http://127.0.0.1:3000` | Node.js 后端（PM2 进程名 `xuewaiyu-backend`） |
| WEB 端 | `https://127.0.0.1`（Host: `yandao.vip`，443） | 网页端站点 |
| APP 端 | `http://127.0.0.1:8080` | APP 站点网关 |

---

## 一、部署流程

### 1.1 标准五步部署法概述

部署脚本 `deploy.sh` 将整个发布流程拆分为 8 个主步骤（含若干子步骤），核心逻辑为：**备份 → 拉取代码 → 语法检查 → 依赖安装 → 数据库同步 → nginx 下发 → 前端同步 → 服务重载 → 双端冒烟**。任意一个关键步骤失败都会自动触发回滚（详见第二章）。

部署前务必确认：
- 当前位于项目目录 `/www/xuewaiyu-backend`
- `.env.production` 文件存在且包含正确的 `DATABASE_URL`、`JWT_SECRET`、`HUNYUAN_API_KEY`、`ADMIN_USER_IDS` 等变量
- PostgreSQL、Redis 服务正常运行
- PM2 进程 `xuewaiyu-backend` 已存在（首次部署需先 `pm2 start`）

执行部署：

```bash
cd /www/xuewaiyu-backend
bash deploy.sh
```

### 1.2 步骤 [1/8]：预部署备份三件套

部署脚本在执行任何变更前，先完成三件套备份，确保回滚时有完整快照可用。

脚本首先加载环境变量：

```bash
if [ -f .env.production ]; then
  set -a; source .env.production; set +a
fi
```

**三件套内容：**

| 备份项 | 命令 | 输出文件 | 说明 |
|--------|------|----------|------|
| 数据库全量快照 | `pg_dump -U postgres -F c -b -f "$PRE_DEPLOY_DB" xuewaiyu` | `/www/backups/xuewaiyu_<TIMESTAMP>.dump` | PostgreSQL 自定义格式（`-F c`），含二进制大对象（`-b`） |
| 前端目录快照 | `tar -czf "$PRE_DEPLOY_WWW" -C / "www/xuewaiyu"` | `/www/backups/www_pre_<TIMESTAMP>.tar.gz` | 打包整个前端部署目录 |
| 后端代码快照 | `tar -czf "$PRE_DEPLOY_CODE" -C "$PROJECT_DIR" --exclude=node_modules --exclude=.git .` | `/www/backups/code_pre_<TIMESTAMP>.tar.gz` | 排除 node_modules 和 .git |

其中 `TIMESTAMP=$(date +%Y%m%d_%H%M%S)`，每次部署生成唯一时间戳。

同时记录部署前的 git commit SHA：

```bash
PRE_DEPLOY_HEAD=$(git rev-parse HEAD 2>/dev/null || echo "")
```

> 注意：数据库备份失败不会阻断部署（脚本仅告警），但回滚时将无数据库快照可用。前端/后端快照创建失败同样仅告警。务必在部署日志中确认三项备份均成功生成。

### 1.3 步骤 [2/8]：git pull + 版本一致性校验

```bash
git stash 2>/dev/null || true
git pull origin main 2>&1 | tail -3
```

**版本一致性校验**（杜绝拉取失败静默降级）：

```bash
NEW_HEAD=$(git rev-parse HEAD 2>/dev/null || echo "")
REMOTE_HEAD=$(git ls-remote origin main 2>/dev/null | awk '{print $1}')
if [ -n "$REMOTE_HEAD" ] && [ "$NEW_HEAD" != "$REMOTE_HEAD" ]; then
  fail "版本不一致：本地 $NEW_HEAD != 远端 $REMOTE_HEAD（疑似拉取失败）"
fi
```

- `git pull` 失败（`PIPESTATUS[0] != 0`）→ 立即触发回滚
- 本地 HEAD 与远端 `origin/main` 不一致 → 立即触发回滚

### 1.4 步骤 [2.5/8]：语法全量检查（bash -n + node --check）

在部署操作前先做静态语法检查，提前暴露语法错误，避免「拉取成功但代码有语法错 → PM2 启动即崩 → 全站 500」的哑弹部署。

**Shell 语法检查**（`bash -n`）：

检查范围为 `deploy.sh` 自身 + 仓库内 `maxdepth 2` 的所有 `.sh` 文件（排除 node_modules）：

```bash
for shf in deploy.sh $(find . -maxdepth 2 -name "*.sh" -not -path "./node_modules/*" 2>/dev/null); do
  bash -n "$shf" >/dev/null 2>&1
done
```

任一 shell 脚本存在语法错误 → 触发回滚，部署中止。

**JS 语法检查**（`node --check`）：

检查范围为 `maxdepth 3` 内的路由、控制器、服务文件（匹配 `*.route.js`、`*Controller.js`、`*.service.js`，排除 node_modules）：

```bash
for jsf in $(find . -maxdepth 3 \( -name "*.route.js" -o -name "*Controller.js" -o -name "*.service.js" \) -not -path "./node_modules/*" 2>/dev/null); do
  node --check "$jsf" >/dev/null 2>&1
done
```

任一 JS 文件存在语法错误 → 触发回滚，部署中止。

### 1.5 步骤 [3/8]：npm install --omit=dev

```bash
npm install --omit=dev 2>&1 | tail -3
```

仅安装生产依赖（跳过 devDependencies），减少安装体积和潜在安全风险。

### 1.6 步骤 [4/8]：prisma db push + prisma generate

```bash
set -a; source .env.production; set +a
npx prisma db push --accept-data-loss 2>&1 | tail -5
```

- `prisma db push --accept-data-loss`：将 `schema.prisma` 的结构变更同步到数据库（注意 `--accept-data-loss` 标志，列类型变更可能丢数据，务必确认 schema 变更影响）
- `prisma db push` 失败 → 触发回滚
- `prisma generate` 失败 → 触发回滚（Prisma Client 未更新会导致后续请求崩溃）

### 1.7 步骤 [4a/8]：vocabulary seed 导入

```bash
if [ -f "prisma/seeds/vocabulary.seed.js" ]; then
  node prisma/seeds/vocabulary.seed.js 2>&1 | tail -3
  VOCAB_CNT=$(node prisma/seeds/vocabulary.count.js 2>/dev/null | tail -1)
  if [ "${VOCAB_CNT:-0}" -lt 200 ]; then
    fail "vocabulary size < 200 (got ${VOCAB_CNT:-0}); practice cannot serve questions"
  fi
fi
```

- seed 脚本按 `(language, word)` 幂等 upsert，每次部署安全执行
- seed 导入失败 → 触发回滚（词汇模块将不可用）
- 词库数量 < 200 → 触发回滚（抽题接口无法返回题目）

### 1.8 步骤 [4b/8]：存量数据兼容性补全（status IS NULL → active）

```bash
BACKFILL=$(psql "$DATABASE_URL" -t -A -c \
  "UPDATE \"User\" SET status='active' WHERE status IS NULL; SELECT count(*) FROM \"User\" WHERE status IS NULL;" 2>&1)
```

**背景**：`db push` 新增带默认值的列时，存量行取值依数据库/驱动实现而异，不能假设一定回填。此处显式将 `status IS NULL` 的用户补为 `'active'`，确保存量用户不因 status 为 NULL 被误判为不可用。

- 代码层已用 `isUserUsable()` 兼容 NULL，此处为数据层双保险
- psql 不可用或列不存在时仅告警，不阻断部署

### 1.9 步骤 [4c/8]：冒烟非管理员账号（幂等创建）

部署脚本通过内联 Node 脚本确保冒烟专用非管理员账号存在：

- 冒烟非管理员手机号：`SMOKE_NONADMIN_PHONE`（默认 `13900001234`）
- 密码复用主冒烟账号（`SMOKE_ADMIN_PHONE` 默认 `13480010005`）的哈希
- 该账号仅用于部署冒烟验证 403 权限门禁，不新增明文口令

### 1.10 步骤 [4d/8]：nginx 配置下发 + 语法校验

这是双端发布强化的核心步骤（对应 2026-08-03 APP 403 事故整改）。

**① 备份现有 nginx vhost 配置**（供回滚 0/4 使用）：

```bash
mkdir -p "$NGINX_BAK_DIR"
cp -a "$NGINX_VHOST_DIR"/*.conf "$NGINX_BAK_DIR"/ 2>/dev/null || true
```

备份目录：`/www/backups/nginx_<TIMESTAMP>`

**② 下发 git 真值源中的 nginx 配置**（`deploy/nginx/*.conf`）：

主配置识别与跳过机制——vhost 目录会被主配置以 `include vhost/nginx/*.conf` 全量包含，只能存放 `server{}` 级别配置。脚本按内容特征识别并隔离主配置：

```bash
# 主配置特征：顶层出现 worker_processes / events { / http {
if grep -qE '^[[:space:]]*(worker_processes|events[[:space:]]*\{|http[[:space:]]*\{)' "$cf"; then
  echo "  ⏭ 跳过主配置（不可下发到 vhost）: $fname"
  continue
fi

# 必须包含 server{} 才算合法 vhost 配置
if ! grep -qE '^[[:space:]]*server[[:space:]]*\{' "$cf"; then
  echo "  ⏭ 跳过非 vhost 配置（无 server 块）: $fname"
  continue
fi

cp -f "$cf" "$NGINX_VHOST_DIR/$fname"
```

**③ 隔离机制**——清理 vhost 目录中残留的历史误下发主配置：

```bash
for vf in "$NGINX_VHOST_DIR"/*.conf; do
  if grep -qE '^[[:space:]]*(worker_processes|events[[:space:]]*\{)' "$vf"; then
    mkdir -p "$NGINX_BAK_DIR/quarantine"
    mv -f "$vf" "$NGINX_BAK_DIR/quarantine/$(basename "$vf")"
  fi
done
```

隔离目录：`/www/backups/nginx_<TIMESTAMP>/quarantine/`

**④ conf.d 子目录下发**（如有 `deploy/nginx/conf.d/`）：

```bash
if [ -d "$NGINX_CONF_SRC/conf.d" ]; then
  mkdir -p "$NGINX_VHOST_DIR/conf.d"
  cp -af "$NGINX_CONF_SRC/conf.d"/* "$NGINX_VHOST_DIR/conf.d"/ 2>/dev/null || true
fi
```

**⑤ 语法校验**：

```bash
if nginx -t >/dev/null 2>&1; then
  echo "  ✓ nginx 语法校验通过"
else
  fail "nginx 配置语法错误，已回滚至部署前版本"
fi
```

`nginx -t` 不通过 → 触发回滚（下发错误的配置比不下发还糟）。

**当前 git 真值源中的 nginx 配置文件：**

| 文件 | 站点 | 端口 | 说明 |
|------|------|------|------|
| `deploy/nginx/yandao.vip.conf` | `yandao.vip` / `www.yandao.vip` | 80 + 443 SSL | 网页端主站，HTTP→HTTPS 跳转 |
| `deploy/nginx/new-app.conf` | APP 站点网关 | 8080 | APP 端站点，含 `/api` 反代 + `/xuewaiyu/` alias |
| `deploy/nginx/82.156.228.87.conf` | IP 直连 + admin | 80 + 443 | IP 直连站点 + 管理后台 |
| `deploy/nginx/conf.d/ailos.conf` | `api.ailos.com` | 80 + 443 | API 站点配置（限流 + 安全头） |
| `deploy/nginx/nginx-main/nginx.conf` | 主配置 | - | **会被脚本识别并跳过**，不下发到 vhost |

> 重要：禁止直接在服务器修改 nginx 配置文件。任何变更必须先提交到 `deploy/nginx/` 的 git 真值源，再走发布脚本统一下发。

### 1.11 步骤 [5/8]：rsync 前端同步（方案 A 强制规则）

前端同步采用「方案 A」刚性规则，唯一真值源为 `public/xuewaiyu/`。

**③ 清理残留**（避免旧文件污染，保留运行时 uploads 目录）：

```bash
rm -rf "$WWW_DIR/public"                          # 删除历史 rsync 遗留子目录
for f in "$WWW_DIR"/*.html; do rm -f "$f"; done   # 删除根级零散 HTML
```

**② 公共资源同步**：

```bash
mkdir -p "$WWW_DIR/assets"
cp -rf public/assets/* "$WWW_DIR/assets/"
```

**① 业务页面同步**（唯一真值源，直接覆盖）：

```bash
cp -rf public/xuewaiyu/* "$WWW_DIR/"
```

**版本号注入**（强制浏览器刷新缓存）：

```bash
SHORT_SHA=$(echo "$NEW_HEAD" | cut -c1-8)
find "$WWW_DIR" -maxdepth 1 -name "*.html" -type f -exec \
  sed -i -E "s|(assets/common\.js)(\?v=[A-Za-z0-9]+)?|\1?v=$SHORT_SHA|g" {} \;
```

所有 HTML 中的 `assets/common.js` 会被注入 `?v=<8位SHA>` 版本号。

**部署门禁**（任一不达标触发回滚）：

| 门禁项 | 校验逻辑 | 失败处理 |
|--------|----------|----------|
| `assets/common.js` 存在 | 文件存在性检查 | 回滚 |
| `assets/common.js` 大小 > 20KB | `stat -c%s` 检查，小于 20480 字节判定同步不完整 | 回滚 |
| 默认头像存在 | `assets/images/default_avatar.png` 文件存在 | 回滚 |

### 1.12 步骤 [6/8]：基础设施连通性校验

在重载服务前，真实探活 PostgreSQL 和 Redis（不能只测 `/api/health` 就当基础设施没问题）。

**PostgreSQL 连通性**：

```bash
psql "$DATABASE_URL" -c "SELECT 1;"
```

- 连通（SELECT 1 成功）→ 继续
- 不可连通 → 触发回滚

**Redis 连通性**：

```bash
REDIS_PONG=$(redis-cli ping 2>/dev/null || echo "")
```

- 返回 PONG → 继续
- 未响应 PONG → 仅告警（鉴权黑名单将 fail-open，已注销账号仍由 `isActive` 兜底拦截，但全设备下线会失效，需尽快修复 Redis）

**反馈落盘目录可写性**：

```bash
FEEDBACK_DIR="$PROJECT_DIR/data/feedback"
mkdir -p "$FEEDBACK_DIR"
[ -w "$FEEDBACK_DIR" ]  # 不可写仅告警
```

### 1.13 步骤 [7/8]：PM2 reload

```bash
pm2 reload xuewaiyu-backend --update-env || fail "pm2 reload 失败"
sleep 3
pm2 list 2>/dev/null | grep xuewaiyu
```

- 使用 `reload`（零停机平滑重载）而非 `restart`（强制重启）
- `--update-env` 确保最新环境变量生效
- reload 失败 → 触发回滚

### 1.14 步骤 [8/8]：双端冒烟测试（WEB 443 + APP 8080）

冒烟测试是部署的最后一道门禁，必须双端全绿才允许上线。

**健康检查**（最多等待 30 秒）：

```bash
for i in $(seq 1 30); do
  HC=$(curl -s -o /dev/null -w "%{http_code}" --max-time 3 "$BASE_URL/api/health" 2>/dev/null || echo 000)
  [ "$HC" = "200" ] && break
  sleep 1
done
[ "$HC" != "200" ] && fail "30 秒内 /api/health 未返回 200"
```

**WEB 端核心页面冒烟**（方案 A 强制门禁，任一非 200 触发回滚）：

```
home.html learn.html review.html profile.html membership.html
practice.html game.html feedback.html chat.html discover.html
translate.html vocabulary.html
community-friends.html community-messages.html community-trend.html
```

**核心静态资源硬校验**：

- `xuewaiyu/assets/common.js` → 必须 200
- `xuewaiyu/assets/images/default_avatar.png` → 必须 200

**头像路径合规扫描**：所有线上 HTML 中头像 src 必须统一为 `/xuewaiyu/assets/images/default_avatar.png`，禁止 `/assets/` 开头的错误路径。

**common.js 版本匹配**：页面内 `common.js?v=$SHORT_SHA` 必须与提交 SHA 匹配。

**核心接口冒烟**（必须全部 200）：

1. 登录：`POST /api/auth/login`（账号 `13480010005` / 密码 `Test123456`）→ 必须返回 token
2. 鉴权接口：`user/me`、`dashboard`、`practice/config`、`ai/quota`、`membership/status`
3. 词汇接口：`v1/practice/progress?lang=ja`、`v1/practice/questions?lang=ja&level=beginner&type=choice&limit=5`、`v1/reviews/wrong-questions?lang=ja&page=1&pageSize=10`
4. 抽题校验：抽题接口必须返回真实题目（`word` 字段数 ≥ 1）

**安全校验**：

| 校验项 | 期望结果 | 失败处理 |
|--------|----------|----------|
| 匿名访问 `/api/feedback/list` | 401 | 回滚（数据泄露风险） |
| 非管理员访问 `/api/feedback/list` | 403 | 回滚（权限失效） |
| 管理员访问 `/api/feedback/list` | 200 | 回滚（管理端不可用） |
| 反馈连续第 4 次提交 | 429 | 回滚（限流未生效） |
| 注销后 token 访问 `/api/user/me` | 401 | 回滚（全设备下线失效） |

**版本一致性终检**：

```bash
FINAL_HEAD=$(cd "$PROJECT_DIR" && git rev-parse HEAD)
if [ -n "$REMOTE_HEAD" ] && [ "$FINAL_HEAD" != "$REMOTE_HEAD" ]; then
  fail "部署后版本与远端不一致，部署未真正生效"
fi
```

**APP 端（8080）冒烟**：

- APP 核心页面：`landing.html home.html learn.html profile.html practice.html game.html chat.html`
- APP 登录：`POST /api/auth/login`（经 8080 网关反代到 3000）→ 必须 200
- APP 鉴权接口：`user/me`、`dashboard`、`ai/quota`（必须带 APP 端登录 token）
- APP 词汇接口：`v1/practice/progress?lang=ja`、`v1/practice/questions?lang=ja&level=beginner&type=choice&limit=5`

全部通过后输出部署成功摘要（版本 SHA、静态资源版本、备份位置）。

---

## 二、回滚流程

### 2.1 自动回滚触发条件

部署脚本中所有关键步骤均通过 `fail()` 函数显式判断退出码，失败即调用 `rollback()`。以下任一条件触发自动回滚：

| 阶段 | 触发条件 | 失败原因 |
|------|----------|----------|
| [2/8] 代码拉取 | `git pull origin main` 退出码非 0 | 网络问题、冲突、权限问题 |
| [2/8] 版本校验 | 本地 HEAD ≠ 远端 `origin/main` | 拉取失败静默降级 |
| [2.5/8] 语法检查 | 任一 `.sh` 文件 `bash -n` 失败 | shell 语法错误 |
| [2.5/8] 语法检查 | 任一 JS 文件 `node --check` 失败 | JS 语法错误 |
| [4/8] 数据库同步 | `prisma db push` 退出码非 0 | 数据库结构同步失败 |
| [4/8] 数据库同步 | `prisma generate` 退出码非 0 | Prisma Client 未更新 |
| [4a/8] seed 导入 | vocabulary seed 失败或词库 < 200 | 词汇模块不可用 |
| [4d/8] nginx 校验 | `nginx -t` 失败 | nginx 配置语法错误 |
| [5/8] 前端同步 | 公共资源 / 业务页面同步失败 | cp 失败 |
| [5/8] 部署门禁 | common.js 缺失 / 大小 < 20KB / 默认头像缺失 | 同步不完整 |
| [6/8] 基础设施 | PostgreSQL 不可连通 | 数据库连接失败 |
| [7/8] 服务重载 | `pm2 reload` 失败 | PM2 重载失败 |
| [8/8] 健康检查 | 30 秒内 `/api/health` 未返回 200 | 服务未启动 |
| [8/8] 核心页面 | 任一核心页面非 200 | 前端部署不完整 |
| [8/8] 核心资源 | common.js / 默认头像非 200 | 核心资源不可用 |
| [8/8] 头像路径 | 发现 `/assets/` 开头错误路径 | 违反方案 A 统一路径规则 |
| [8/8] 版本匹配 | common.js 版本号与 SHA 不匹配 | 版本注入失败 |
| [8/8] 核心接口 | 登录未返回 token / 核心接口非 200 | 核心链路异常 |
| [8/8] 词汇接口 | 词汇接口非 200 / 抽题无真实题目 | 词库为空 |
| [8/8] 安全校验 | 匿名非 401 / 非管理员非 403 / 管理员非 200 | 权限失效或数据泄露 |
| [8/8] 安全校验 | 反馈第 4 次非 429 | 限流未生效 |
| [8/8] 安全校验 | 注销后 token 非 401 | 全设备下线失效 |
| [8/8] 版本终检 | 部署后 HEAD ≠ 远端 | 部署未真正生效 |
| [8/8] APP 冒烟 | APP 页面 / 登录 / 接口 / 词汇接口非 200 | APP 站点部署不完整 |

### 2.2 四步回滚机制

当 `fail()` 被调用时，`rollback()` 函数按以下四步执行：

**[0/4] nginx 配置恢复**

```bash
# 剔除备份目录中可能混入的历史误下发主配置（否则回滚反而会把 nginx 弄坏）
for bf in "$NGINX_BAK_DIR"/*.conf; do
  if grep -qE '^[[:space:]]*(worker_processes|events[[:space:]]*\{)' "$bf"; then
    mv -f "$bf" "$bf.disabled"
  fi
done
# 恢复 vhost 配置
cp -a "$NGINX_BAK_DIR"/*.conf "$NGINX_VHOST_DIR"/
# 语法校验后平滑重载
nginx -t && nginx -s reload
```

- 备份目录中混入的主配置会被先剔除（重命名为 `.disabled`），避免回滚污染
- 恢复后再次 `nginx -t` 校验，通过才 reload；未通过则告警要求人工检查

**[1/4] 前端快照恢复**

```bash
rm -rf "$WWW_DIR"
mkdir -p "$WWW_DIR"
tar -xzf "$PRE_DEPLOY_WWW" -C / 2>/dev/null
```

- 删除当前前端目录，从部署前快照解压恢复
- 无快照可用（首次部署）时跳过

**[2/4] 后端代码回退**

```bash
cd "$PROJECT_DIR"
git stash 2>/dev/null || true
git checkout "$PRE_DEPLOY_HEAD"
# 重新加载环境变量 + 重新生成 Prisma Client
source .env.production
npx prisma generate
```

- 回退到部署前记录的 commit SHA（`PRE_DEPLOY_HEAD`）
- 回退后重新 `prisma generate` 确保 Prisma Client 与代码版本一致
- 无部署前 commit 记录时跳过

**[3/4] PM2 重启**

```bash
pm2 reload xuewaiyu-backend --update-env 2>/dev/null || pm2 restart xuewaiyu-backend 2>/dev/null || true
```

- 优先 `reload`（零停机），失败则 `restart`（强制重启）

**[4/4] 二次健康校验**

```bash
for i in $(seq 1 20); do
  rb_hc=$(curl -s -o /dev/null -w "%{http_code}" --max-time 3 "$BASE_URL/api/health" 2>/dev/null || echo 000)
  [ "$rb_hc" = "200" ] && break
  sleep 1
done
```

- 最多重试 20 次（每秒一次），确认回滚后服务真的恢复
- 通过 → 输出「回滚后健康检查通过」
- 未通过 → 输出紧急告警，并列出数据库备份、前端快照、后端快照路径，要求立即人工介入

### 2.3 手动回滚操作步骤

当自动回滚失败或需要手动回滚到特定版本时，按以下步骤操作：

**一键手动回滚**（回退到部署前版本，deploy.sh 末尾输出的命令）：

```bash
cd /www/xuewaiyu-backend && git checkout <PRE_DEPLOY_HEAD> && pm2 reload xuewaiyu-backend --update-env
```

**完整手动回滚（含前端 + nginx）**：

```bash
# 1. 确认要回退的 commit SHA（查看部署日志或 git log）
cd /www/xuewaiyu-backend
git log --oneline -10

# 2. 回退后端代码
git checkout <目标commit_SHA>
source .env.production
npx prisma generate

# 3. 恢复前端快照（从备份目录选择对应时间戳）
ls /www/backups/www_pre_*.tar.gz
rm -rf /www/xuewaiyu
mkdir -p /www/xuewaiyu
tar -xzf /www/backups/www_pre_<TIMESTAMP>.tar.gz -C /

# 4. 恢复 nginx 配置（从备份目录）
ls /www/backups/nginx_*/
cp -a /www/backups/nginx_<TIMESTAMP>/*.conf /www/server/panel/vhost/nginx/
nginx -t && nginx -s reload

# 5. 重启服务并健康校验
pm2 reload xuewaiyu-backend --update-env
sleep 3
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3000/api/health
# 期望输出 200

# 6. 数据库恢复（如需要，从 pg_dump 备份恢复）
pg_restore -U postgres -d xuewaiyu -c /www/backups/xuewaiyu_<TIMESTAMP>.dump
```

---

## 三、备份机制

### 3.1 三件套备份详细说明

每次执行 `deploy.sh` 时，在步骤 [1/8] 自动生成三件套备份：

| 备份项 | 工具 | 格式 | 用途 |
|--------|------|------|------|
| 数据库快照 | `pg_dump -U postgres -F c -b` | PostgreSQL 自定义压缩格式（`.dump`） | 数据库整体恢复 |
| 前端目录快照 | `tar -czf` | gzip 压缩包（`.tar.gz`） | 前端静态资源恢复 |
| 后端代码快照 | `tar -czf`（排除 node_modules、.git） | gzip 压缩包（`.tar.gz`） | 后端代码恢复 |

此外，nginx 配置在步骤 [4d/8] 下发前也会备份：

| 备份项 | 路径 | 用途 |
|--------|------|------|
| nginx vhost 配置 | `/www/backups/nginx_<TIMESTAMP>/` | nginx 配置回滚 |
| 隔离的主配置 | `/www/backups/nginx_<TIMESTAMP>/quarantine/` | 误下发主配置的隔离留存 |

### 3.2 备份文件位置和命名规则

所有备份统一存放在 `/www/backups/` 目录下，命名规则如下：

```
/www/backups/
├── xuewaiyu_20260807_143000.dump              # 数据库快照（pg_dump 自定义格式）
├── www_pre_20260807_143000.tar.gz             # 前端目录快照
├── code_pre_20260807_143000.tar.gz            # 后端代码快照
└── nginx_20260807_143000/                     # nginx 配置备份目录
    ├── *.conf                                  # vhost 配置文件
    ├── quarantine/                             # 隔离的误下发主配置
    │   └── *.conf
    └── *.conf.disabled                         # 回滚时剔除的主配置
```

- `TIMESTAMP` 格式为 `YYYYMMDD_HHMMSS`（如 `20260807_143000`）
- 每次部署生成唯一时间戳，不会覆盖历史备份
- 部署成功后，脚本输出的摘要中会列出本次所有备份文件路径

### 3.3 数据库备份恢复命令

数据库备份使用 PostgreSQL 自定义格式（`-F c`），恢复时使用 `pg_restore`：

```bash
# 完整恢复（会先清空再导入，谨慎操作）
pg_restore -U postgres -d xuewaiyu -c /www/backups/xuewaiyu_<TIMESTAMP>.dump

# 仅查看备份内容（不实际恢复）
pg_restore -l /www/backups/xuewaiyu_<TIMESTAMP>.dump

# 恢复到指定数据库（非 xuewaiyu）
pg_restore -U postgres -d <目标数据库名> /www/backups/xuewaiyu_<TIMESTAMP>.dump

# 恢复时忽略错误继续（用于部分恢复场景）
pg_restore -U postgres -d xuewaiyu --exit-on-error=false /www/backups/xuewaiyu_<TIMESTAMP>.dump
```

> 注意：`-c`（`--clean`）会先删除现有对象再重建，生产环境恢复前务必确认目标数据库正确。恢复后需重启 PM2 服务使 Prisma Client 重新连接。

### 3.4 前端/后端快照恢复命令

**前端快照恢复**（快照打包时使用 `-C /`，解压时也需 `-C /` 以保持路径一致）：

```bash
# 恢复前端静态资源
rm -rf /www/xuewaiyu
mkdir -p /www/xuewaiyu
tar -xzf /www/backups/www_pre_<TIMESTAMP>.tar.gz -C /

# 验证恢复结果
ls -la /www/xuewaiyu/assets/common.js
stat -c%s /www/xuewaiyu/assets/common.js  # 应 > 20480 字节
```

**后端代码快照恢复**（快照打包时排除了 node_modules 和 .git，仅恢复业务代码）：

```bash
# 恢复后端代码（解压到项目目录）
cd /www/xuewaiyu-backend
tar -xzf /www/backups/code_pre_<TIMESTAMP>.tar.gz -C /www/xuewaiyu-backend

# 恢复后需重新安装依赖并生成 Prisma Client
npm install --omit=dev
npx prisma generate

# 重启服务
pm2 reload xuewaiyu-backend --update-env
```

> 注意：后端代码快照不含 node_modules 和 .git，如需回退到特定 git commit，请使用 `git checkout` 方式（见 2.3 节）。代码快照适用于 git 仓库损坏等极端场景。

---

## 四、日常运维

### 4.1 PM2 服务管理命令

后端服务通过 PM2 管理，进程名为 `xuewaiyu-backend`。

```bash
# 查看服务状态
pm2 list
pm2 show xuewaiyu-backend

# 平滑重载（零停机，部署脚本使用的方式，更新环境变量）
pm2 reload xuewaiyu-backend --update-env

# 强制重启（会短暂中断服务）
pm2 restart xuewaiyu-backend

# 停止服务
pm2 stop xuewaiyu-backend

# 启动服务（首次部署或停止后启动）
pm2 start src/server/index.js --name xuewaiyu-backend

# 查看实时日志（默认输出最近 15 行并持续跟踪）
pm2 logs xuewaiyu-backend

# 查看最近 100 行日志
pm2 logs xuewaiyu-backend --lines 100

# 刷新日志（日志轮转时使用）
pm2 flush xuewaiyu-backend

# 重置重启计数
pm2 reset xuewaiyu-backend

# 设置开机自启
pm2 save
pm2 startup
```

### 4.2 日志查看与排查

**应用日志**（Winston，位于项目 `logs/` 目录）：

| 日志文件 | 路径 | 说明 |
|----------|------|------|
| 错误日志 | `/www/xuewaiyu-backend/logs/error.log` | 仅 error 级别，优先排查 |
| 全量日志 | `/www/xuewaiyu-backend/logs/combined.log` | 所有级别日志 |
| PM2 日志 | `~/.pm2/logs/xuewaiyu-backend-out.log` | PM2 标准输出 |
| PM2 错误 | `~/.pm2/logs/xuewaiyu-backend-error.log` | PM2 标准错误 |

日志配置（来自 `src/utils/logger.js`）：
- 日志格式：JSON（含 timestamp、level、message、service 元数据）
- 单文件最大 5MB，最多保留 5 个文件（自动轮转）
- 日志级别由 `LOG_LEVEL` 环境变量控制（默认 `info`，可设为 `debug` 排查问题）
- 生产环境不输出控制台日志，仅写文件

```bash
# 查看应用错误日志
tail -f /www/xuewaiyu-backend/logs/error.log

# 搜索特定关键词
grep "newsCrawlJob" /www/xuewaiyu-backend/logs/combined.log | tail -50

# 查看 PM2 日志
pm2 logs xuewaiyu-backend --lines 200

# 搜索 500 错误
grep '"level":"error"' /www/xuewaiyu-backend/logs/combined.log | tail -20
```

### 4.3 nginx 配置管理

**nginx 配置真值源**：`/www/xuewaiyu-backend/deploy/nginx/`

**nginx vhost 部署目录**（宝塔面板）：`/www/server/panel/vhost/nginx/`

```bash
# 语法校验（修改配置后必须执行）
nginx -t

# 平滑重载（不中断连接）
nginx -s reload

# 查看 nginx 进程
ps aux | grep nginx

# 查看 nginx 版本和编译参数
nginx -V

# 测试指定配置文件
nginx -t -c /www/server/panel/vhost/nginx/yandao.vip.conf
```

**站点与端口对应关系：**

| 站点 | 配置文件 | 监听端口 | 后端反代 |
|------|----------|----------|----------|
| 网页端主站 | `yandao.vip.conf` | 80 → 301 HTTPS / 443 SSL | `/api/` → `127.0.0.1:3000` |
| APP 端网关 | `new-app.conf` | 8080 | `/api/` → `127.0.0.1:3000` |
| IP 直连 | `82.156.228.87.conf` | 80 / 443 SSL | `/api/` → `127.0.0.1:3000` |
| API 站点 | `conf.d/ailos.conf` | 80 / 443 SSL | `/api/` → upstream |

**nginx 访问/错误日志位置：**

```bash
# 主站访问日志
tail -f /www/wwwlogs/yandao.vip.access.log    # 如有配置
# 主站错误日志
tail -f /www/wwwlogs/yandao.vip.error.log

# APP 站点日志（new-app.conf 未单独配置 access_log，使用默认）
# IP 直连站点日志
tail -f /www/wwwlogs/yandao-app.access.log
tail -f /www/wwwlogs/yandao-app.error.log
```

> 重要规范：禁止直接在服务器修改 nginx 配置。所有变更必须先提交到 git 仓库 `deploy/nginx/` 目录，再通过 `deploy.sh` 下发。直接修改服务器文件会在下次部署时被覆盖。

### 4.4 数据库连接与查询

数据库为 PostgreSQL，连接字符串通过 `DATABASE_URL` 环境变量配置（`.env.production`）。

```bash
# 加载环境变量
cd /www/xuewaiyu-backend
set -a; source .env.production; set +a

# 直连数据库
psql "$DATABASE_URL"

# 执行单条查询
psql "$DATABASE_URL" -c "SELECT count(*) FROM \"User\";"

# 查看资讯来源
psql "$DATABASE_URL" -c "SELECT id, name, \"isActive\", \"isBlacklist\", \"dailyLimit\" FROM news_sources;"

# 查看待审核资讯数量
psql "$DATABASE_URL" -c "SELECT status, count(*) FROM news_articles GROUP BY status;"

# 查看用户状态分布
psql "$DATABASE_URL" -c "SELECT status, count(*) FROM \"User\" GROUP BY status;"

# 连通性测试
psql "$DATABASE_URL" -c "SELECT 1;"
```

**Prisma 命令：**

```bash
# 查看数据库状态（对比 schema 与实际数据库差异）
npx prisma migrate status

# 同步 schema 到数据库（部署脚本使用的方式）
npx prisma db push --accept-data-loss

# 重新生成 Prisma Client
npx prisma generate

# 打开 Prisma Studio（可视化数据库管理，生产环境谨慎使用）
npx prisma studio
```

### 4.5 Redis 缓存管理

Redis 配置通过 `REDIS_HOST`、`REDIS_PORT`、`REDIS_PASSWORD` 环境变量管理（默认 `localhost:6379`）。

Redis 在系统中的用途：JWT token 黑名单（注销/全设备下线）、限流计数器、缓存。

```bash
# 连通性测试
redis-cli ping
# 期望返回 PONG

# 带密码连接
redis-cli -h <REDIS_HOST> -p <REDIS_PORT> -a <REDIS_PASSWORD> ping

# 查看所有 key（谨慎，生产环境 key 可能很多）
redis-cli keys "*"

# 查看 token 黑名单相关 key（注销后 token 失效依赖此项）
redis-cli keys "blacklist:*"

# 查看特定 key 的类型和值
redis-cli type <key>
redis-cli get <key>

# 查看内存使用
redis-cli info memory

# 查看连接数
redis-cli info clients

# 清空当前数据库（危险操作，仅排障时使用）
redis-cli flushdb
```

> 注意：Redis 不可用时，鉴权黑名单将 fail-open（已注销账号仍由数据库 `isActive` 字段兜底拦截），但全设备下线功能会失效。部署脚本中 Redis 未响应 PONG 仅告警不阻断，但应尽快修复。

---

## 五、故障排查

### 5.1 常见故障与解决方案

#### 故障 1：PM2 进程崩溃 / 频繁重启

**现象**：`pm2 list` 显示 `errored` 或 `restart` 次数异常增长，服务不可用。

**排查步骤**：

```bash
# 查看 PM2 错误日志
pm2 logs xuewaiyu-backend --err --lines 100

# 查看应用错误日志
tail -100 /www/xuewaiyu-backend/logs/error.log

# 检查端口是否被占用
lsof -i :3000

# 检查环境变量是否加载（.env.production 是否存在且完整）
cd /www/xuewaiyu-backend
cat .env.production | grep -E "DATABASE_URL|JWT_SECRET|HUNYUAN_API_KEY"
```

**常见原因与解决**：
- `.env.production` 缺失或变量不完整 → 补全环境变量后重启
- `node_modules` 不完整或 Prisma Client 未生成 → `npm install --omit=dev && npx prisma generate`
- 端口 3000 被占用 → `lsof -i :3000` 查找并杀掉占用进程
- 代码语法错误 → 重新部署（部署脚本会自动回滚）

#### 故障 2：nginx 502 Bad Gateway

**现象**：网页端或 APP 端返回 502。

**原因**：nginx 无法连接后端服务（`127.0.0.1:3000`）。

**排查步骤**：

```bash
# 1. 检查后端服务是否运行
pm2 list | grep xuewaiyu
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3000/api/health
# 期望 200

# 2. 检查 nginx 错误日志
tail -50 /www/wwwlogs/yandao.vip.error.log
tail -50 /www/wwwlogs/yandao-app.error.log

# 3. 检查 nginx 配置语法
nginx -t

# 4. 若后端未运行，启动服务
pm2 restart xuewaiyu-backend
```

#### 故障 3：nginx 503 Service Unavailable

**现象**：返回 503。

**原因**：后端服务过载或限流触发。

**排查步骤**：

```bash
# 检查后端服务负载
pm2 monit

# 检查系统资源
top
df -h
free -m

# 检查是否有大量请求堆积
pm2 logs xuewaiyu-backend --lines 50
```

#### 故障 4：数据库连接失败

**现象**：API 返回 500，日志中出现 `PrismaClientInitializationError` 或数据库连接超时。

**排查步骤**：

```bash
# 1. 测试数据库连通性
cd /www/xuewaiyu-backend
set -a; source .env.production; set +a
psql "$DATABASE_URL" -c "SELECT 1;"

# 2. 检查 PostgreSQL 服务状态
systemctl status postgresql
# 或
service postgresql status

# 3. 检查 DATABASE_URL 是否正确
echo $DATABASE_URL

# 4. 若数据库服务未运行，启动
systemctl start postgresql
```

#### 故障 5：Redis 连接失败

**现象**：注销后 token 仍可用（全设备下线失效），日志中出现 Redis 连接错误。

**排查步骤**：

```bash
# 1. 测试 Redis 连通性
redis-cli ping
# 期望 PONG

# 2. 检查 Redis 服务状态
systemctl status redis
# 或
service redis status

# 3. 若 Redis 未运行，启动
systemctl start redis

# 4. 检查 Redis 配置（密码等）
redis-cli info server
```

> 注意：Redis 不可用时系统不会完全瘫痪（fail-open 机制），但安全相关功能（token 黑名单、全设备下线）会失效，应尽快修复。

#### 故障 6：API 返回 500 内部错误

**现象**：特定接口返回 500。

**排查步骤**：

```bash
# 1. 查看应用错误日志（优先）
tail -100 /www/xuewaiyu-backend/logs/error.log

# 2. 查看 PM2 错误日志
pm2 logs xuewaiyu-backend --err --lines 50

# 3. 复现问题并查看完整请求日志
curl -v http://127.0.0.1:3000/api/<接口路径>

# 4. 检查数据库相关表是否存在
psql "$DATABASE_URL" -c "\dt"

# 5. 若为 Prisma Client 版本不匹配
cd /www/xuewaiyu-backend
npx prisma generate
pm2 reload xuewaiyu-backend --update-env
```

### 5.2 健康检查接口

系统提供健康检查接口，用于快速判断服务状态：

```bash
# 后端健康检查（直连后端，绕过 nginx）
curl http://127.0.0.1:3000/api/health
# 返回: {"success":true,"status":"healthy","timestamp":"..."}

# WEB 端健康检查（经 nginx 443）
curl -sk -H "Host: yandao.vip" https://127.0.0.1/api/health

# APP 端健康检查（经 nginx 8080）
curl http://127.0.0.1:8080/api/health

# 仅获取 HTTP 状态码
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3000/api/health
# 期望: 200
```

健康检查接口定义在 `src/server/routes/index.js`，返回 JSON 格式 `{ success: true, status: 'healthy', timestamp: <ISO时间> }`。该接口不受限流影响（rate limiter 中 `skip: (req) => req.path === '/health'`）。

### 5.3 日志文件位置汇总

| 日志类型 | 文件路径 | 说明 |
|----------|----------|------|
| 应用错误日志 | `/www/xuewaiyu-backend/logs/error.log` | Winston error 级别，5MB 轮转 |
| 应用全量日志 | `/www/xuewaiyu-backend/logs/combined.log` | Winston 全级别，5MB 轮转 |
| PM2 标准输出 | `~/.pm2/logs/xuewaiyu-backend-out.log` | console.log 输出 |
| PM2 错误输出 | `~/.pm2/logs/xuewaiyu-backend-error.log` | console.error 输出 |
| nginx 主站日志 | `/www/wwwlogs/yandao.vip.*.log` | 主站访问/错误 |
| nginx APP 日志 | `/www/wwwlogs/yandao-app.*.log` | IP 直连站点访问/错误 |
| nginx 默认日志 | `/var/log/nginx/` 或宝塔默认路径 | 其他 nginx 日志 |
| 反馈落盘目录 | `/www/xuewaiyu-backend/data/feedback/` | 用户反馈数据 |

---

## 六、v3.2.0 新增模块运维

### 6.1 资讯抓取定时任务管理

v3.2.0 新增站外资讯定时增量抓取任务，代码位于 `src/jobs/newsCrawlJob.js`，服务启动时自动注册（`src/server/index.js` 第 89 行）。

**调度配置：**

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `NEWS_CRAWL_CRON` | `0 6,18 * * *` | cron 表达式，默认每日 06:00 和 18:00 各执行一次 |
| `NEWS_CRAWL_JOB_DISABLED` | `0` | 设为 `1` 则禁用定时任务（不启动） |
| `TZ` | `Asia/Shanghai` | 时区，影响 cron 调度时间 |

**cron 表达式说明**（5 字段标准格式）：

```
┌──────── 分钟 (0-59)
│ ┌────── 小时 (0-23)
│ │ ┌──── 日 (1-31)
│ │ │ ┌── 月 (1-12)
│ │ │ │ ┌ 星期 (0-7, 0和7均为周日)
│ │ │ │ │
0 6,18 * * *    # 每天 06:00 和 18:00 执行
```

**修改调度频率**（修改 `.env.production` 后需重启服务生效）：

```bash
# 示例：改为每天 08:00 执行一次
# 编辑 .env.production，设置：
# NEWS_CRAWL_CRON=0 8 * * *

# 重启服务使配置生效
pm2 reload xuewaiyu-backend --update-env
```

**禁用定时任务**：

```bash
# 方式一：环境变量禁用（推荐，重启后生效）
# 在 .env.production 中设置：
# NEWS_CRAWL_JOB_DISABLED=1
pm2 reload xuewaiyu-backend --update-env

# 方式二：测试环境自动不启动（代码中 NODE_ENV === 'test' 时跳过）
```

**手动触发抓取**（管理员接口，需登录管理员账号）：

```bash
# 获取管理员 token
TOKEN=$(curl -s -X POST http://127.0.0.1:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"account":"13480010005","password":"Test123456"}' \
  | python3 -c "import sys,json;print(json.load(sys.stdin).get('tokens',{}).get('accessToken',''))")

# 手动触发抓取（默认关闭 AI）
curl -s -X POST http://127.0.0.1:3000/api/v1/news/crawl \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"enableAI":false}'

# 手动触发抓取（开启 AI 辅助过滤，消耗 AI 额度）
curl -s -X POST http://127.0.0.1:3000/api/v1/news/crawl \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"enableAI":true}'
```

接口路径：`POST /api/v1/news/crawl`（挂载于 `src/server/routes/news.js`，需 `authenticate` + `requireAdmin` 中间件）。

返回示例：

```json
{
  "success": true,
  "data": {
    "sourcesProcessed": 3,
    "totalStored": 15,
    "totalFiltered": 8,
    "totalDuplicated": 2,
    "durationMs": 5234
  },
  "message": "抓取完成"
}
```

**抓取逻辑说明**（来自 `src/services/newsAggregatorService.js`）：

1. 遍历所有 `isActive=true` 且 `isBlacklist=false` 的资讯来源
2. 检查单来源每日抓取次数是否超限（`dailyLimit`，默认 10）
3. 解析来源的 `feedUrl`（支持 RSS 2.0 / Atom / JSON Feed 格式）
4. 带指数退避的重试机制（最多 3 次，延迟 1s/2s/4s，上限 5s）
5. 三层过滤：关键词规则 → 白名单 → AI 辅助（可选）
6. 敏感词过滤（复用 `contentFilter`）
7. 基于内容哈希（`MD5(标题|URL)`）增量去重
8. 自动分类（基于标题/摘要关键词匹配：learning_method / study_abroad / exam_policy / culture / hotspot / general）
9. 所有通过过滤的文章以 `status='pending'` 进入待审核池

### 6.2 AI 开关管理

资讯模块的 AI 辅助过滤功能受双重管控：环境变量初始值 + 运行时动态切换。

**环境变量配置**（来自 `src/services/newsFilterService.js`）：

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `NEWS_AI_ENABLED` | `false`（未设为 `true` 时均为关闭） | AI 辅助过滤全局开关初始值 |

**双重管控逻辑**：

```javascript
// newsFilterService.js
const aiActuallyEnabled = enableAI && _aiEnabledGlobal;
```

- `enableAI`：调用时传入的参数（定时任务自动读取全局状态，手动触发可在 body 中指定）
- `_aiEnabledGlobal`：全局 AI 开关状态（初始值来自 `NEWS_AI_ENABLED`，运行时可动态修改）
- 两者均为 `true` 时 AI 才实际启用

**后台动态切换**（管理员接口，无需重启服务）：

```bash
# 获取管理员 token（同上）
TOKEN=...

# 查询当前 AI 开关状态
curl -s http://127.0.0.1:3000/api/admin/news/ai-status \
  -H "Authorization: Bearer $TOKEN"
# 返回: {"success":true,"data":{"aiEnabled":false}}

# 开启 AI 辅助过滤
curl -s -X POST http://127.0.0.1:3000/api/admin/news/ai-toggle \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"enabled":true}'
# 返回: {"success":true,"data":{"aiEnabled":true},"message":"AI辅助过滤已开启"}

# 关闭 AI 辅助过滤
curl -s -X POST http://127.0.0.1:3000/api/admin/news/ai-toggle \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"enabled":false}'
# 返回: {"success":true,"data":{"aiEnabled":false},"message":"AI辅助过滤已关闭"}
```

接口路径：
- `GET /api/admin/news/ai-status` — 获取全局 AI 开关状态
- `POST /api/admin/news/ai-toggle` — 切换全局 AI 开关（body: `{ "enabled": true|false }`）

> 注意：
> - 运行时切换的状态仅保存在内存中，服务重启后会恢复为 `NEWS_AI_ENABLED` 环境变量的值。如需永久开启，需同时修改 `.env.production` 中的 `NEWS_AI_ENABLED=true`。
> - AI 辅助过滤通过 `BrainFacade` 统一调度层调用大模型（合规要求：禁止直连大模型），单条资讯 AI 调用限制 1 次，纳入平台 AI 配额管理。
> - AI 调用会写入 `NewsAuditLog`（action=`ai_process`），实现操作级审计追溯。
> - 默认关闭 AI 是合规要求（《双宪法v3.2.0》任务五：默认关闭 AI 深度处理，零 AI 额度消耗）。

### 6.3 资讯审核流程操作指南

所有资讯管理接口挂载于 `/api/admin/news`，需管理员权限（`authenticate` + `requireAdmin`）。

**资讯文章状态流转**（`status` 字段）：

```
pending（待审核） ──approve──→ approved（已通过，对外展示）
pending（待审核） ──reject───→ rejected（已拒绝）
approved（已通过） ──offline──→ offline（已下架）
任意状态 ──举报确认──→ offline（已下架）
```

**审核操作命令**：

```bash
# 获取管理员 token
TOKEN=$(curl -s -X POST http://127.0.0.1:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"account":"13480010005","password":"Test123456"}' \
  | python3 -c "import sys,json;print(json.load(sys.stdin).get('tokens',{}).get('accessToken',''))")

# 查看待审核资讯列表
curl -s "http://127.0.0.1:3000/api/admin/news/articles?status=pending&page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"

# 查看所有状态资讯
curl -s "http://127.0.0.1:3000/api/admin/news/articles?page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"

# 按分类筛选
curl -s "http://127.0.0.1:3000/api/admin/news/articles?category=learning_method&page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"

# 审核通过
curl -s -X POST http://127.0.0.1:3000/api/admin/news/articles/<文章ID>/approve \
  -H "Authorization: Bearer $TOKEN"

# 审核拒绝（可附原因）
curl -s -X POST http://127.0.0.1:3000/api/admin/news/articles/<文章ID>/reject \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"reason":"内容不合规"}'

# 下架已发布资讯
curl -s -X POST http://127.0.0.1:3000/api/admin/news/articles/<文章ID>/offline \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"reason":"违规内容"}'

# 置顶
curl -s -X POST http://127.0.0.1:3000/api/admin/news/articles/<文章ID>/pin \
  -H "Authorization: Bearer $TOKEN"

# 取消置顶
curl -s -X POST http://127.0.0.1:3000/api/admin/news/articles/<文章ID>/unpin \
  -H "Authorization: Bearer $TOKEN"

# 投放官方广告（仅白名单来源允许）
curl -s -X POST http://127.0.0.1:3000/api/admin/news/articles/<文章ID>/ad-place \
  -H "Authorization: Bearer $TOKEN"

# 撤销官方广告
curl -s -X POST http://127.0.0.1:3000/api/admin/news/articles/<文章ID>/ad-remove \
  -H "Authorization: Bearer $TOKEN"
```

**资讯来源管理**：

```bash
# 查看所有来源
curl -s http://127.0.0.1:3000/api/admin/news/sources \
  -H "Authorization: Bearer $TOKEN"

# 新增来源
curl -s -X POST http://127.0.0.1:3000/api/admin/news/sources \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "name": "示例来源",
    "url": "https://example.com",
    "feedUrl": "https://example.com/rss.xml",
    "category": "learning_method",
    "language": "zh-CN",
    "isActive": true,
    "isWhitelist": false,
    "dailyLimit": 10
  }'

# 更新来源
curl -s -X PUT http://127.0.0.1:3000/api/admin/news/sources/<来源ID> \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"dailyLimit": 20}'

# 拉黑来源（停止抓取）
curl -s -X POST http://127.0.0.1:3000/api/admin/news/sources/<来源ID>/block \
  -H "Authorization: Bearer $TOKEN"

# 解除拉黑
curl -s -X POST http://127.0.0.1:3000/api/admin/news/sources/<来源ID>/unblock \
  -H "Authorization: Bearer $TOKEN"

# 删除来源
curl -s -X DELETE http://127.0.0.1:3000/api/admin/news/sources/<来源ID> \
  -H "Authorization: Bearer $TOKEN"
```

**举报处理**：

```bash
# 查看待处理举报列表
curl -s "http://127.0.0.1:3000/api/admin/news/reports?status=pending&page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"

# 确认举报违规（自动下架文章）
curl -s -X POST http://127.0.0.1:3000/api/admin/news/reports/<举报ID>/resolve \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"action":"resolve","reason":"确认违规"}'

# 驳回举报（文章不下架）
curl -s -X POST http://127.0.0.1:3000/api/admin/news/reports/<举报ID>/resolve \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"action":"dismiss"}'
```

**统计概览**：

```bash
curl -s http://127.0.0.1:3000/api/admin/news/stats \
  -H "Authorization: Bearer $TOKEN"
```

返回示例：

```json
{
  "success": true,
  "data": {
    "sources": { "total": 5, "active": 3 },
    "articles": { "total": 120, "pending": 15, "approved": 95 },
    "reports": { "total": 8, "pending": 2 },
    "aiCalls": { "total": 23, "enabled": false }
  }
}
```

### 6.4 审计日志查询

资讯模块的所有管理操作均写入审计日志（`NewsAuditLog` 模型，数据库表 `news_audit_logs`），实现操作级追溯。

**审计日志记录的操作类型**（`action` 字段）：

| action | 说明 | 触发接口 |
|--------|------|----------|
| `approve` | 审核通过 | `POST /articles/:id/approve` |
| `reject` | 审核拒绝 | `POST /articles/:id/reject` |
| `offline` | 下架 | `POST /articles/:id/offline` |
| `pin` | 置顶 | `POST /articles/:id/pin` |
| `unpin` | 取消置顶 | `POST /articles/:id/unpin` |
| `ad_place` | 投放官方广告 | `POST /articles/:id/ad-place` |
| `ad_remove` | 撤销官方广告 | `POST /articles/:id/ad-remove` |
| `source_create` | 新增来源 | `POST /sources` |
| `source_update` | 更新来源 | `PUT /sources/:id` |
| `source_block` | 拉黑来源 | `POST /sources/:id/block` |
| `source_unblock` | 解除拉黑 | `POST /sources/:id/unblock` |
| `source_delete` | 删除来源 | `DELETE /sources/:id` |
| `ai_process` | AI 处理记录 | AI 辅助过滤自动写入 |
| `ai_toggle` | AI 开关切换 | `POST /ai-toggle` |

**通过 API 查询审计日志**：

```bash
# 获取管理员 token（同上）
TOKEN=...

# 查询全部审计日志（分页）
curl -s "http://127.0.0.1:3000/api/admin/news/audit-logs?page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"

# 按 action 类型筛选（如查询所有审核操作）
curl -s "http://127.0.0.1:3000/api/admin/news/audit-logs?action=approve&page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"

# 查询 AI 开关切换记录
curl -s "http://127.0.0.1:3000/api/admin/news/audit-logs?action=ai_toggle&page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"

# 查询 AI 处理记录
curl -s "http://127.0.0.1:3000/api/admin/news/audit-logs?action=ai_process&page=1&limit=20" \
  -H "Authorization: Bearer $TOKEN"
```

返回示例：

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "uuid",
        "articleId": "uuid",
        "operatorId": "用户ID",
        "action": "approve",
        "beforeState": "pending",
        "afterState": "approved",
        "detail": null,
        "createdAt": "2026-08-07T10:00:00.000Z",
        "operator": { "id": "用户ID", "nickname": "管理员" },
        "article": { "id": "uuid", "title": "文章标题" }
      }
    ],
    "pagination": { "page": 1, "limit": 20, "total": 50, "totalPages": 3 }
  }
}
```

**通过数据库直接查询**：

```bash
cd /www/xuewaiyu-backend
set -a; source .env.production; set +a

# 查看最近 20 条审计日志
psql "$DATABASE_URL" -c \
  "SELECT action, \"operatorId\", \"beforeState\", \"afterState\", \"createdAt\" FROM news_audit_logs ORDER BY \"createdAt\" DESC LIMIT 20;"

# 按操作类型统计
psql "$DATABASE_URL" -c \
  "SELECT action, count(*) FROM news_audit_logs GROUP BY action ORDER BY count DESC;"

# 查询特定管理员的操作记录
psql "$DATABASE_URL" -c \
  "SELECT action, \"articleId\", \"createdAt\" FROM news_audit_logs WHERE \"operatorId\" = '<用户ID>' ORDER BY \"createdAt\" DESC;"

# 查询今日审计日志
psql "$DATABASE_URL" -c \
  "SELECT action, \"operatorId\", \"createdAt\" FROM news_audit_logs WHERE \"createdAt\" >= CURRENT_DATE ORDER BY \"createdAt\" DESC;"
```

**审计日志字段说明**（`NewsAuditLog` 模型，表 `news_audit_logs`）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | String (UUID) | 主键 |
| `articleId` | String? | 关联文章 ID（来源管理类操作为 null） |
| `operatorId` | String? | 操作人用户 ID（AI 自动操作为 null） |
| `action` | String | 操作类型（见上表） |
| `beforeState` | String? | 操作前状态 |
| `afterState` | String? | 操作后状态 |
| `detail` | Json? | 操作详情（JSON，如拒绝原因、变更内容等） |
| `createdAt` | DateTime | 操作时间 |

---

## 附录：部署成功输出示例

部署全部通过后，脚本输出如下摘要：

```
╔══════════════════════════════════════════╗
║  ✅ 部署完成（所有门禁校验通过）         ║
╚══════════════════════════════════════════╝
  版本:         <FINAL_HEAD>
  静态资源版本: common.js?v=<SHORT_SHA>
  数据库备份:   /www/backups/xuewaiyu_<TIMESTAMP>.dump
  前端快照:     /www/backups/www_pre_<TIMESTAMP>.tar.gz
  后端快照:     /www/backups/code_pre_<TIMESTAMP>.tar.gz
  手动回滚:     cd /www/xuewaiyu-backend && git checkout <PRE_DEPLOY_HEAD> && pm2 reload xuewaiyu-backend --update-env
```

请妥善保存此输出中的备份路径和回滚命令，以备紧急回滚使用。
