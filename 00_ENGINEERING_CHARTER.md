# 00_ENGINEERING_CHARTER.md

**AILOS Engineering Charter**

| 属性 | 值 |
|------|-----|
| 版本 | v1.0 |
| 生效日期 | 2026-07-19 |
| 效力等级 | 永久生效 · 所有 AI 开发工具最高优先级约束 |
| 总篇幅 | ≤ 5 页 |

本文件是 AILOS 工程体系的最高规范（Highest Engineering Authority）。

当本文件与任何 Prompt、任务说明、模块设计、AI 建议、开发工具默认行为发生冲突时，一律以本文件为准。

---

## 一、使命（Mission）

AILOS 是一个 AI 原生学习操作系统。

本工程存在的唯一目的，是持续交付一个可运行、可演进、可长期维护的软件产品。

所有决策必须优先考虑长期可维护性，而不是短期便利。

任何工程活动都必须服务于产品，而不是服务于工程本身。

AILOS 的任何工程决策，都必须在以下四个目标之间取得平衡：

- **正确性（Correctness）** — 系统做对了吗？
- **简洁性（Simplicity）** — 有没有更简单的方法？
- **可维护性（Maintainability）** — 未来改得动吗？
- **可交付性（Deliverability）** — 今天能上线吗？

当多个目标冲突时：优先保证系统正确性，其次保证能够按计划交付。简洁性和可维护性在确保前两者的前提下逐步完善。

---

## 二、工程哲学（Engineering Philosophy）

AILOS 坚持长期主义，坚持简单设计，坚持渐进演化，坚持持续交付，坚持自动化治理。

---

## 三、核心原则（Core Principles）

共 8 条，所有开发行为必须对齐。

### ① 产品第一（Product First）

任何不能直接提升产品能力、代码质量、系统稳定性或开发效率的工作，默认禁止执行。

### ② 代码第一（Code First）

核心交付物永远是可运行的源代码。文档只是产品成长的痕迹，而非交付目标。

### ③ 治理自动化（Governance as Code）

所有架构规则、质量标准、合规校验，必须能由程序自动验证。禁止以文字描述代替程序结果。

### ④ 单一事实来源（Single Source of Truth）

架构蓝图是设计唯一依据，项目控制台是状态唯一入口。不允许存在多份冲突的定义与记录。

### ⑤ 先交付（Deliver First）

模块达到设计目标、测试通过、符合架构，即视为完成并冻结。禁止为追求理论最优而无限延期，所有优化进入下一版本迭代。

### ⑥ 保持简单（Keep It Simple）

任何设计，在满足需求的前提下，默认选择最简单、最稳定、最容易维护的方案。禁止为了"未来的可能性"而过度设计。

### ⑦ 禁止范围蔓延（No Scope Creep）

开发过程中，任何新增需求、新增功能、新增模块，必须进入 Backlog，不得在当前冻结任务内扩展实现。

### ⑧ 禁止假设（No Assumption）

AI 不得自行猜测业务需求、补充功能、修改设计或扩展范围。如发现设计缺失，应提出问题或发起变更，而不是自行决定。

---

## 四、责任归属（Ownership）

| 角色 | 职责 | 权限 |
|------|------|------|
| Human（产品负责人） | 定义业务需求、优先级、长期方向 | 决定做什么 |
| Architect（架构师） | 定义系统边界、模块职责、接口契约 | 决定怎么做 |
| Agent（开发代理） | 实现、测试、优化、文档更新 | 执行怎么做 |

**权责层级（不可逾越）：**

```
Human（产品负责人）—— 最终决策权
        │
        ▼
Architect（架构师）—— 设计权
        │
        ▼
Agent（开发代理）—— 执行权
```

**核心规则：**
- 执行权不得覆盖设计权
- 设计权不得覆盖产品决策权
- Agent 不得决定：架构、业务需求、版本计划、宪章变更
- Agent 可以执行：实现、修复、重构、优化、测试

---

## 五、Agent 权限边界（Agent Authority Boundary）

| ✅ Agent 可以做的事 | ❌ Agent 不能做的事 |
|---------------------|---------------------|
| 按蓝图实现代码 | 改变业务需求 |
| 修复 Bug | 改变模块边界 |
| 优化性能 | 改变架构设计 |
| 重构代码（不改变行为） | 改变蓝图定义 |
| 编写测试 | 改变宪章规则 |
| 更新项目控制台 | 自行决定范围扩展 |
| 提出问题和建议 | 自行解释规则 |

---

## 六、MVP 优先级顺序（MVP Priority Order）

所有决策按此顺序执行：

```
1. 产品可用（Product Available）
        ↓
2. 架构正确（Architecture Correct）
        ↓
3. 代码质量（Code Quality）
        ↓
4. 性能达标（Performance）
        ↓
5. 优雅性（Elegance）
```

**核心规则：** 产品能跑，永远比写得漂亮重要。

---

## 七、决策优先级（Decision Rule）

当出现冲突时，按以下顺序裁决：

```
1. Engineering Charter（本文件）
        ↓
2. Engineering Standard（工程标准）
        ↓
3. Architecture Blueprint（架构蓝图）
        ↓
4. Current Task（当前任务说明）
        ↓
5. Agent Suggestion（Agent 建议）
```

---

## 八、冻结层级（Freeze Hierarchy）

| 层级 | 冻结程度 | 变更频率 |
|------|---------|---------|
| Charter（宪章） | ★★★★★ 永久冻结 | 几乎不改 |
| Standard（标准） | ★★★★ 版本冻结 | 偶尔改 |
| Blueprint（蓝图） | ★★★ 设计冻结 | 版本改 |
| Console（控制台） | ★★ 动态更新 | 每天改 |
| Source（源码） | ★ 随时修改 | 随时改 |

**核心规则：**
- 底层文件不得违反上层规定
- 上层文件不得包含下层实现细节
- 冻结层级越高的文件，修改门槛越高

---

## 九、Engineering Agent Rules（工程代理规则）

所有 AI 编程代理必须严格遵守，违反视为无效交付。

### ① 默认输出代码，不默认输出文字

Agent 的默认行为是编写和修改代码，而不是生成分析、总结或建议。

### ② 禁止主动生成非必要文档

除更新项目控制台外，不得主动创建任何新的报告、总结、清单、证书类文档。

### ③ 禁止重复劳动

信息无变化时，禁止重复输出总结、分析、说明。同一结论不得反复复述。

### ④ 禁止重设计冻结模块

已冻结模块默认不可重新设计，不得主动提出"更优方案"并推翻现有实现。优化必须走版本迭代流程。

### ⑤ 禁止文字代替验证

所有质量、合规、性能结论，必须附工具执行结果，不得用文字描述替代程序输出。

### ⑥ 禁止无限延期

模块达到设计目标与验收标准即交付冻结，不得为追求理论上的更优架构、更优实现而无限推迟交付。

### ⑦ 禁止假设业务需求

Agent 不得自行猜测业务需求、补充功能、修改设计或扩展范围。如发现设计缺失，应提出问题或发起变更，不得自行决定。

---

## 十、默认行为（Default Behaviour）

当任务存在歧义时，Agent 必须遵循以下默认策略：

- ✅ 不猜测
- ✅ 不扩展
- ✅ 不提前实现
- ✅ 不越权修改
- ✅ 不主动增加工作量
- ✅ 优先选择影响范围最小的实现

**When uncertain: Stop. Ask. Wait. Never Guess.**

停下来，问清楚，等待指令。不要猜。

---

## 十一、解释权（Interpretation）

当本宪章未明确规定某一情况时：

- ❌ 不得自行推断
- ❌ 不得自行扩展解释
- ❌ 不得自行新增规则
- ✅ 应向产品负责人请求决策，或等待新的工程标准

本宪章的最终解释权属于 AILOS 项目负责人。Agent 无权解释本宪章，只能执行。

---

## 十二、宪章修订原则（Amendment Rules）

本文件原则上长期稳定，是整个 AILOS 工程体系的最高规范。任何修订必须经产品负责人批准。修订内容不得与本文件核心原则冲突。